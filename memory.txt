【必须执行】

1. 正式进入 Phase 3 开发。
   用户文档补充完成，必须阅读phase3的全部文档，继续推进Phase 3 平台化管理能力开发。

2. 每次开始新开发前，先读文档，再编码。
   最少先看：
   - 00_project_index.md
   - enterprise_web_test_platform_tech_design.md
   - enterprise_web_test_platform_implementation_design.md
   - enterprise_web_test_platform_java_core_code_skeleton.md
   - cdp_domain_encapsulation_detailed_design.md
   - edge_extension_native_messaging_protocol_detailed_design.md
   - edge_extension_typescript_protocol_and_code_skeleton.md
   - 01_dev_progress.md
   - 本次任务直接相关文档

3. 读完文档后，必须先输出理解总结，再进入编码。
   必须覆盖：
   - 项目目标
   - 当前进度
   - 已完成 / 未完成模块
   - 本次任务涉及模块、接口、协议、文档依据
   - 实现思路
   - 风险和不确定点
   - 计划修改文件

4. 开发时必须遵守现有架构和协议。
   - 不擅自改模块边界
   - 若改接口或协议，先说明原因和影响
   - 优先最小改动、可运行、可验证

5. 开发完成后必须更新 01_dev_progress.md。
   至少写入：
   - 本次完成内容
   - 修改文件
   - 当前状态
   - 已知问题
   - 下一步计划


【未来参考】

1. Phase 1 / Phase 2 核心链路已做过一轮完整扩展，包含：
   - locator / action / wait / assertion / artifact 等能力
   - 真实 Edge DSL smoke 已多次通过
   - 常见验证命令一直是 Maven 测试、package、install，以及 apps/core-platform 下的 smoke 执行

2. 已完成过的断言与等待能力摘要：
   - ASSERT_TITLE / ASSERT_URL
   - ASSERT_TEXT / ASSERT_VISIBLE / ASSERT_NOT_VISIBLE
   - ASSERT_VALUE / ASSERT_ATTR
   - WAIT_FOR_ELEMENT / WAIT_FOR_VISIBLE / WAIT_FOR_HIDDEN / WAIT_FOR_URL
   - SCREENSHOT 已从 orchestrator 抽到 artifact-engine

3. Phase 3 近况摘要：
   -UI完全需要重构，应该类似常用的点击侧栏可切换操作页面的后台操作系统，同时支持中日英三语言切换，UIdemo必须参考docs/phase3/platform_and_edge_low_fidelity_wireframes.md，禁止修改后台逻辑
   - local-admin-api 已提供本地快照读取与部分写入接口
   - admin-console 已支持调度写入、配置编辑、项目编辑、用例编辑
   - Edge popup 已接到 native-host 桥，不再直接调用本地 HTTP
   - native-host 已支持打包、安装、卸载脚本

4. 最近已完成的 Phase 3 事项：
   - native-host 参数解析兼容 Edge 启动参数
   - Maven 已能产出可运行的 jar-with-dependencies
   - 已提供：
     - scripts/install-native-host.ps1
     - install-native-host.bat
     - scripts/uninstall-native-host.ps1
     - uninstall-native-host.bat
   - 文档已同步到 docs/phase3/phase3_local_admin_api_mock_contracts.md

5. 最近建议过、但属于未来参考的后续方向：
   - 增加 native-host 的 origin 校验
   - 提供打包后的可执行启动器
   - 回到 case/project 管理，补 delete / archive filter / picker / richer validation
   - 把 file-backed 持久化逐步替换成真实运行态服务

6. 如果后面继续开发，先检查“文档要求”和“Phase 3阶段的下一步开发步骤”这两个前置条件，再决定是否继续。

7. 2026-04-19 UI demo / UI重构补充记忆：
   - `uidemo/edge-self-test-demo.html` 是 Phase 3 UI 重构的直接参考实现，不只是视觉稿，而是带组件拆分建议、主题切换、语言切换、11个核心屏幕的交互 demo。
   - demo 内已经明确可迁移结构：
     - theme tokens：LIGHT / DARK / FONT / ThemeContext / useTheme / 文案函数 `L()`
     - ui-kit：Logo / TopBar / Sidebar / Card / Button / Badge / Tabs / BrowserChrome / CodeBlock 等
     - screens：Dashboard、Projects、Case detail、Doc parse、AI generate、Exec monitor、Report detail、Data diff、Model config、Data templates、Plugin popup
     - Frame：通用外壳 = TopBar + Sidebar + children
   - 文档依据仍然是 `docs/phase3/platform_and_edge_low_fidelity_wireframes.md`，demo 是在线框图基础上的结构化落地稿；后续 UI 开发必须同时对齐“线框图信息架构”和“demo 组件拆分方式”。
   - 当前 `ui/admin-console` 现状仍偏 Phase 3 shell：
     - 主要逻辑集中在 `ui/admin-console/src/App.tsx`
     - 主要样式集中在 `ui/admin-console/src/styles.css`
     - 已有 dashboard / projects / cases / execution / reports / models / environments 等壳层能力
     - 但还不是完整后台操作系统形态，距离目标侧栏切页、统一 frame、多语言、主题化、插件协同页面仍有明显差距
   - 后续 UI 重构原则：
     - 先做信息架构和页面骨架重组，再迁移现有表单/接口能力
     - 先拆 Frame、tokens、ui-kit、screen，再拆业务数据访问
     - 平台后台与 Edge popup 要共用设计语言，但保持各自布局职责
     - 至少预留中/日/英三语言切换，不再继续做单语言页面
     - 优先复用现有 local-admin-api 接口，不先改协议

8. 2026-04-19 admin-console 第一轮 UI 重构完成补充记忆：
   - `ui/admin-console/src/App.tsx` 已从长页面 shell 重组为 Phase 3 frame：TopBar + Sidebar + Main workspace。
   - 当前已具备 sidebar 切屏：
     - dashboard
     - projects
     - cases
     - execution
     - reports
     - models
     - environments
   - 当前已具备：
     - light / dark theme switch
     - 中文 / 日本語 / English 三语言切换
     - 更接近 `uidemo/edge-self-test-demo.html` 的后台工作台层级
   - 本轮没有改后端协议，只重做前端展示层与结构层；现有写接口都保持兼容。
   - 验证已通过：
     - `npm test -- --run` in `ui/admin-console`
     - `npm run build` in `ui/admin-console`
   - 已完成（截至 2026-04-19）：
     - 已正式拆成 `Frame` / `ui-kit` / `screens` 多文件
     - 已迁入 doc parse / AI generate / exec monitor / report detail 等完整 demo 屏幕
     - project / case 管理 delete / archive filter / picker / richer validation 已完成
   - 仍未完成：
     - snapshot 里的业务文案部分未完整国际化（其余均已完成）
12. 2026-04-19 Cases / Execution pre-execution UI refactor constraint
   - Strict rule for the current stage: front-end UI refactor only.
   - Do not modify any backend logic, backend contract, backend API behavior, native-host logic, or local-admin-api logic for this flow.
   - `Cases` detail `Run` is redefined as `Pre-execution` in the UI flow only.
   - `Execution` uses front-end state only to show prepared case count after `Run ID` + `Project` selection; clicking `Execution` starts the prepared UI flow only on the existing front-end wiring.
   - This restriction must be followed strictly until the user explicitly reopens backend work.
13. 2026-04-20 Phase 3 documentation stage constraint
   - The dashboard UI refactor and the Edge plugin UI refactor are considered completed for the current stage.
   - The current stage is documentation only. Do not modify backend logic, backend contracts, local-admin-api behavior, native-host logic, front-end UI structure, or front-end interaction behavior.
   - New work should focus on defining two deliverables for each real screen:
     - a detailed functional description document
     - a detailed interface document
   - All new screen documents must be generated under `docs/phase3/interface`.
   - In `docs/phase3/interface`, each screen must use its own folder named by the screen name.
   - The functional description for each screen must cover:
     - screen purpose and operator value
     - visible modules and interaction areas
     - field meanings, actions, states, and validation rules already implied by the current UI
     - relations with upstream and downstream screens
     - data dependencies and interface dependencies
   - The interface document for each screen must cover:
     - read interfaces used by the screen
     - write interfaces used by the screen
   - request and response structures already implied by the current implementation and Phase 3 docs
   - ownership boundaries between screen UI, app state, and backend endpoints
   - If documentation reveals a missing function or a UI gap, it may be recorded as a review item only. No UI change may be implemented before user review.
   - The first documentation target should start from `dashboard`, because it is the overview entry and the hub that references queue, reports, risks, model policy, and operational status.
14. 2026-04-20 Dashboard documentation package completed
   - Generated the first screen folder under `docs/phase3/interface/dashboard`.
   - Generated:
     - `docs/phase3/interface/dashboard/functional-spec.md`
     - `docs/phase3/interface/dashboard/interface-spec.md`
   - The dashboard documents explicitly distinguish:
     - current implementation behavior
     - intended screen responsibility
     - cross-screen relationships
     - app-level snapshot dependency on `GET /api/phase3/admin-console`
   - Review items were recorded in the docs only:
     - `Refresh` button is visible but not wired
     - `New run` button is visible but not wired to `execution`
     - the page still renders mostly screen-local static overview data instead of mapping shell snapshot fields deeply
   - These review items must remain documentation-only until user review allows UI changes.
15. 2026-04-20 Dashboard interface spec regenerated with backend architecture context
   - Re-read all documents under `docs/phase3/main` to align the screen interface doc with the full Phase 3 platform design.
   - The regenerated `docs/phase3/interface/dashboard/interface-spec.md` now distinguishes:
     - current implemented local-admin-api endpoints
     - backend module construction and ownership
     - dashboard direct interface vs indirect source interfaces
     - local file-backed state sources aggregated into the admin snapshot
     - native-host / extension sibling architecture
     - future planned management API evolution from the main design docs
   - The document now treats `GET /api/phase3/admin-console` as the dashboard direct read interface and explains in detail how it aggregates:
     - scheduler request/event state
     - catalog state
     - model/environment config state
     - execution history and local report state
   - The document also explains the detailed function of the related Phase 3 mutation interfaces:
     - `POST /api/phase3/scheduler/requests`
     - `POST /api/phase3/scheduler/events`
     - `POST /api/phase3/config/model`
     - `POST /api/phase3/config/environment`
     - `POST /api/phase3/catalog/project`
     - `POST /api/phase3/catalog/case`
16. 2026-04-20 Dashboard interface spec supplemented with control-level mapping
   - The dashboard interface document now contains a dedicated control-to-interface mapping section.
   - For each visible dashboard control, the document now states:
     - control location
     - user action
     - intended request or route
     - owning module
     - success/failure feedback expectation
     - current implementation status
   - This clarified that dashboard currently has:
     - visible but unwired action buttons
     - read-only summary cards and panels
     - no dashboard-local input field or dropdown that submits payloads directly
   - Use the same structure for later screen interface docs so each page documents not only API endpoints but also which button or control should invoke them.
17. 2026-04-20 Dashboard functional spec supplemented for review depth
   - The dashboard functional document now also includes:
     - screen input definition
     - screen output definition
     - functional control responsibility matrix
   - This makes the functional document align better with the interface document:
     - functional-spec explains what each area and control is for
     - interface-spec explains what each control should request or route to
   - Reuse this structure for later pages so functional-spec is not only a page overview, but also a reviewable function description at control granularity.
18. 2026-04-20 Projects documentation package completed
   - Generated the second screen folder under `docs/phase3/interface/projects`.
   - Generated:
     - `docs/phase3/interface/projects/functional-spec.md`
     - `docs/phase3/interface/projects/interface-spec.md`
   - The projects documents follow the same depth as dashboard:
     - functional-spec includes purpose, areas, inputs/outputs, state model, validation, cross-screen relations, and control responsibility matrix
     - interface-spec includes direct read/write interfaces, persistence reflection path, local-only controls, control-to-interface mapping, and future API evolution notes
   - The projects interface document now explicitly distinguishes:
     - direct read from `GET /api/phase3/admin-console`
     - direct write to `POST /api/phase3/catalog/project`
     - local-only controls such as search, card selection, open/close detail, row add/remove
     - visible but still unwired controls such as `Import`, `New project`, `Enter project`, and `View reports`
19. 2026-04-20 Projects unwired controls now have concrete implementation design
   - The projects interface document no longer stops at “unimplemented”.
   - It now gives direct design for each unwired control:
     - `New project` reuses existing local row-add flow and existing `POST /api/phase3/catalog/project`
     - `Import` gets a detailed preview/commit import API design
     - card `Reports` and inline `View reports` use route-state handoff into `reports`, not a new backend API
     - inline `Enter project` uses route-state handoff into `cases`, not a new backend API
   - Rule going forward:
     - if a placeholder control can reuse an existing interface or route-state handoff, prefer that
     - only design a new backend endpoint when the control introduces a truly new backend responsibility such as import/preview/commit
20. 2026-04-20 Dashboard unwired controls also supplemented with concrete implementation design
   - The dashboard interface document now applies the same standard as projects.
   - It no longer stops at “intended behavior” for unwired controls.
   - Concrete implementation design was added for:
     - `Refresh`
     - `New run`
     - recent run row click
     - attention item click
     - AI summary/provider chip click
   - The design reuses existing app-level state and route handlers where possible:
     - `loadSnapshot()`
     - `handleScreenChange("execution" | "monitor" | "models")`
     - `openReportDetail(runName)`
     - `openDataDiff(runName)`
   - Rule reinforced:
     - dashboard overview controls should prefer route-state and existing shell reload logic
     - they should not introduce new backend endpoints unless the screen truly creates a new backend responsibility
21. 2026-04-20 Cases documentation package completed
   - Generated the third screen folder under `docs/phase3/interface/cases`.
   - Generated:
     - `docs/phase3/interface/cases/functional-spec.md`
     - `docs/phase3/interface/cases/interface-spec.md`
   - The cases functional document now distinguishes:
     - current visible behavior as a project-scoped case overview plus lower detail canvas
     - the real implemented `Pre-execution` handoff into `execution`
     - the current absence of a visible case catalog editor even though app-level case save capability exists
     - the intended relation to DSL, state-machine, plans, and history maintenance
   - The cases interface document now distinguishes:
     - direct read from `GET /api/phase3/admin-console`
     - state-only `Pre-execution` handoff from `cases` to `execution`
     - app-level case persistence through `POST /api/phase3/catalog/case`
     - new proposed detailed interfaces required by currently unwired controls:
       - `GET /api/phase3/cases/{caseId}/dsl`
       - `POST /api/phase3/cases/{caseId}/dsl/validate`
       - `PUT /api/phase3/cases/{caseId}/dsl`
       - `GET /api/phase3/cases/{caseId}/state-machine`
       - `GET /api/phase3/cases/{caseId}/plans`
       - `GET /api/phase3/cases/{caseId}/history`
   - Review conclusion for this screen:
     - the current UI is a read-and-handoff surface, not yet a full case-management editor surface
     - placeholder controls now have concrete implementation design instead of only "unwired" notes
22. 2026-04-20 Execution documentation package completed
   - Generated the fourth screen folder under `docs/phase3/interface/execution`.
   - Generated:
     - `docs/phase3/interface/execution/functional-spec.md`
     - `docs/phase3/interface/execution/interface-spec.md`
   - The execution functional document now distinguishes:
     - real scheduler mutation ownership on `Run`, `Execution`, and `Open Audit`
     - app-state-only prepared-case input from `cases`
     - mixed data-source design where queue/model/environment context comes from the shared snapshot but compare templates are still front-end seeded
     - current visible but unwired controls such as the header contract-hint button and display-only queue/prepared-case cards
   - The execution interface document now distinguishes:
     - direct read from `GET /api/phase3/admin-console`
     - direct write to `POST /api/phase3/scheduler/requests`
     - direct write to `POST /api/phase3/scheduler/events`
     - current app-state prepared-case boundary
     - current front-end-seeded compare-template boundary
   - Concrete implementation design was added for currently unwired execution controls:
     - header execution-contract hint button as a local contract-help drawer
     - queue row drill-down via route-state to `monitor` or `reportDetail`
     - prepared-case card drill-down via route-state back to `cases`
     - future backend-backed compare-template read interface `GET /api/phase3/data-templates`
     - future runtime detail interface `GET /api/phase3/runs/{runId}/status`
23. 2026-04-20 Monitor documentation package completed
   - Generated the fifth screen folder under `docs/phase3/interface/monitor`.
   - Generated:
     - `docs/phase3/interface/monitor/functional-spec.md`
     - `docs/phase3/interface/monitor/interface-spec.md`
   - The monitor functional document now distinguishes:
     - the current UI shell for runtime observation
     - the very limited real snapshot context currently used by the page
     - the hardcoded/local placeholder nature of `runId`, progress, step timeline, live page, and AI runtime log
     - visible but unwired runtime-control actions `Pause` and `Abort`
   - The monitor interface document now distinguishes:
     - current shallow read context from `GET /api/phase3/admin-console`
     - the absence of any true run-specific runtime payload in the current screen props
     - the need for explicit route-state handoff from `execution` to `monitor` carrying `runId`
   - Concrete implementation design was added for currently unwired monitor controls and missing monitor data contracts:
     - `GET /api/phase3/runs/{runId}/status`
     - `GET /api/phase3/runs/{runId}/steps`
     - `GET /api/phase3/runs/{runId}/runtime-log`
     - `GET /api/phase3/runs/{runId}/live-page`
     - `POST /api/phase3/runs/{runId}/pause`
     - `POST /api/phase3/runs/{runId}/abort`
     - local step/log drill-down behavior
24. 2026-04-20 Reports documentation package completed
   - Generated the sixth screen folder under `docs/phase3/interface/reports`.
   - Generated:
     - `docs/phase3/interface/reports/functional-spec.md`
     - `docs/phase3/interface/reports/interface-spec.md`
   - The reports functional document now distinguishes:
     - project-first report browsing and run-detail entry responsibilities
     - the implemented overview collapse, project switch, and `Detail` actions
     - the display-only operator timeline side panel
   - The reports interface document now distinguishes:
     - direct read from `GET /api/phase3/admin-console`
     - route-state handoff into `reportDetail` via selected run name
     - the front-end-derived nature of the current report list view model built in `reportViewModel.ts`
   - Review conclusion for this screen:
     - many visible list-row fields are currently heuristic/synthetic UI fields rather than stable backend report-list contract fields
     - `Detail` is already a real implemented route-state handoff and should remain lightweight on the list page
   - Added concrete future report interface design for:
     - `GET /api/phase3/runs`
     - `GET /api/phase3/runs/{runId}/report-summary`
     - `GET /api/phase3/runs/{runId}/report`
     - timeline-item route-state drill-down design
25. 2026-04-20 ReportDetail documentation package completed
   - Generated the seventh screen folder under `docs/phase3/interface/reportDetail`.
   - Generated:
     - `docs/phase3/interface/reportDetail/functional-spec.md`
     - `docs/phase3/interface/reportDetail/interface-spec.md`
   - The report-detail functional document now distinguishes:
     - the current role of the page as the one-run detail drill-down page after `reports`
     - the implemented back-to-reports behavior and `Data diff` handoff
     - the visible but unwired hero actions `Download artifacts` and `Re-run`
     - the visible tab structure where only `Data diff` is currently actionable
   - The report-detail interface document now distinguishes:
     - current read context from `GET /api/phase3/admin-console`
     - route-state handoff from `reports` via selected run name
     - current synthetic detail layer built from `selectReportViewModel(...)`
     - future backend-authored report detail interfaces for full report, steps, assertions, recovery, AI decisions, and artifacts
   - Concrete implementation design was added for currently unwired controls:
     - `Download artifacts`
     - `Re-run`
     - tabs other than `Data diff`
   - Review conclusion for this screen:
     - `reportDetail` is now clearly defined as the first page that should eventually move from synthetic UI detail to true backend-authored `report.json` / `report.html` driven detail
26. 2026-04-20 DataDiff documentation package completed
   - Generated the eighth screen folder under `docs/phase3/interface/dataDiff`.
   - Generated:
     - `docs/phase3/interface/dataDiff/functional-spec.md`
     - `docs/phase3/interface/dataDiff/interface-spec.md`
   - The data-diff functional document now distinguishes:
     - the current page purpose as a one-run before/after/after_restore comparison page
     - the current selected-run route-state entry from `reportDetail`
     - the visible but unwired hero actions `View raw JSON` and `Re-restore`
     - the local placeholder nature of the current diff table and stats
   - The data-diff interface document now distinguishes:
     - current shallow read context from `GET /api/phase3/admin-console`
     - the synthetic diff-row layer generated by `makeDiffRows(report)`
     - the future backend-authored run-diff and restore-result interfaces required by this page
   - Concrete implementation design was added for:
     - `GET /api/phase3/runs/{runId}/data-diff`
     - `GET /api/phase3/runs/{runId}/data-diff/raw`
     - `GET /api/phase3/runs/{runId}/restore-result`
     - `POST /api/phase3/runs/{runId}/restore/retry`
     - local raw-json drawer behavior
27. 2026-04-20 DocParse documentation package completed
   - Generated the ninth screen folder under `docs/phase3/interface/docParse`.
   - Generated:
     - `docs/phase3/interface/docParse/functional-spec.md`
     - `docs/phase3/interface/docParse/interface-spec.md`
   - The doc-parse functional document now distinguishes:
     - the current page role as the document-ingestion and parse-review screen upstream of `aiGenerate`
     - the implemented project switch, document open, tab switch, and route-state generate handoff
     - the visible but unwired actions `Re-parse` and `Manual edit`
     - the local-only nature of file upload, raw document, and version-history data
   - The doc-parse interface document now distinguishes:
     - current indirect dependence on `GET /api/phase3/admin-console`
     - the synthetic document catalog created by `buildDocuments(snapshot)`
     - the real route-state contract from `docParse` into `aiGenerate`
   - Concrete future interface design was added for:
     - `GET /api/phase3/documents`
     - `POST /api/phase3/documents/upload`
     - `GET /api/phase3/documents/{documentId}/parse-result`
     - `GET /api/phase3/documents/{documentId}/raw`
     - `GET /api/phase3/documents/{documentId}/versions`
     - `POST /api/phase3/documents/{documentId}/reparse`
     - `PUT /api/phase3/documents/{documentId}/parse-result`
28. 2026-04-20 AiGenerate documentation package completed
   - Generated the tenth screen folder under `docs/phase3/interface/aiGenerate`.
   - Generated:
     - `docs/phase3/interface/aiGenerate/functional-spec.md`
     - `docs/phase3/interface/aiGenerate/interface-spec.md`
   - The ai-generate functional document now distinguishes:
     - the current page role as the generation-review screen between `docParse` and future `cases` / `execution`
     - the implemented candidate-tab switching and focus fallback behavior
     - the placeholder nature of the current flow tree, state machine, and DSL surfaces
     - the visible but unwired actions `Regenerate`, `Dry-run`, and `Save as case`
   - The ai-generate interface document now distinguishes:
     - current route-state handoff from `docParse`
     - current snapshot fallback dependence when no focus exists
     - the reuse boundary for `POST /api/phase3/catalog/case` as the final save path
   - Concrete future interface design was added for:
     - `POST /api/phase3/agent/generate-case`
     - `POST /api/phase3/cases/dsl/validate`
     - `POST /api/phase3/agent/generate-case/dry-run`
     - `POST /api/phase3/catalog/case` submission shape for generated-case persistence
29. 2026-04-20 DataTemplates documentation package completed
   - Generated the eleventh screen folder under `docs/phase3/interface/dataTemplates`.
   - Generated:
     - `docs/phase3/interface/dataTemplates/functional-spec.md`
     - `docs/phase3/interface/dataTemplates/interface-spec.md`
   - The data-templates functional document now distinguishes:
     - the current page role as the reusable data-template catalog and safety-review screen
     - the implemented template-row selection and detail rendering behavior
     - the visible but unwired actions `Import`, `New template`, `Edit`, and `Dry-run`
     - the downstream influence of this screen on `execution` and `dataDiff`
   - The data-templates interface document now distinguishes:
     - the current local template source `defaultDataTemplates` in `App.tsx`
     - the indirect snapshot use of `GET /api/phase3/admin-console` only for project-name lookup
     - the current cross-screen coupling where `execution` consumes the same local template list
   - Concrete future interface design was added for:
     - `GET /api/phase3/data-templates`
     - `GET /api/phase3/data-templates/{templateId}`
     - `POST /api/phase3/data-templates`
     - `PUT /api/phase3/data-templates/{templateId}`
     - `POST /api/phase3/data-templates/import/preview`
     - `POST /api/phase3/data-templates/import/commit`
     - `POST /api/phase3/data-templates/{templateId}/dry-run`
30. 2026-04-20 Models documentation package completed
   - Generated the twelfth screen folder under `docs/phase3/interface/models`.
   - Generated:
     - `docs/phase3/interface/models/functional-spec.md`
     - `docs/phase3/interface/models/interface-spec.md`
   - The models functional document now distinguishes:
     - the current page role as the AI provider and routing-policy configuration screen
     - the implemented provider modal add/edit/delete draft flow
     - the implemented footer save mutation
     - the visible but unwired routing-rule edit action
   - The models interface document now distinguishes:
     - current read from `GET /api/phase3/admin-console`
     - current save through repeated `POST /api/phase3/config/model`
     - current generic config-item persistence into `config/phase3/model-config.json`
     - the local-only nature of provider connection testing via front-end timeout and field presence checks
   - Concrete future interface design was added for:
     - `POST /api/phase3/config/model/test-connection`
     - `GET /api/phase3/models`
     - `PUT /api/phase3/models`
     - routing-rule edit flow reusing the current save pipeline
31. 2026-04-20 Environments documentation package completed
   - Generated the thirteenth screen folder under `docs/phase3/interface/environments`.
   - Generated:
     - `docs/phase3/interface/environments/functional-spec.md`
     - `docs/phase3/interface/environments/interface-spec.md`
   - The environments functional document now distinguishes:
     - the current page role as the datasource/database configuration screen under the `environments` navigation item
     - the implemented create/edit dialog flow
     - the implemented immediate-persist save and delete actions
     - the local-only nature of connection testing
   - The environments interface document now distinguishes:
     - current read from `GET /api/phase3/admin-console`
     - current save through repeated `POST /api/phase3/config/environment`
     - current generic config-item persistence into `config/phase3/environment-config.json`
     - the mismatch between the page name `environments` and the actual UI scope, which currently manages only datasource/database configs
   - Concrete future interface design was added for:
     - `GET /api/phase3/datasources`
     - `PUT /api/phase3/datasources`
     - `POST /api/phase3/datasources/test-connection`
     - `GET /api/phase3/environments`
32. 2026-04-20 Plugin screen classification clarified
   - `plugin` must be treated as the Edge extension front-end template shell, not as a platform business-management screen.
   - Future `docs/phase3/interface/plugin/*` documents must describe it as the UI/template surface for the Edge plugin front end.
   - Review and interface design for this screen should focus on extension-popup UI responsibilities, native-messaging boundaries, and extension-side interaction entry points.
33. 2026-04-20 Plugin documentation package completed
   - Generated the fourteenth screen folder under `docs/phase3/interface/plugin`.
   - Generated:
     - `docs/phase3/interface/plugin/functional-spec.md`
     - `docs/phase3/interface/plugin/interface-spec.md`
   - The plugin functional document now distinguishes:
     - the current page role as the Edge extension front-end template shell rather than a platform business-management screen
     - the intended popup responsibilities of page summary, active run visibility, quick actions, and locator pick mode
     - the currently static/demo nature of all quick actions and picker controls
   - The plugin interface document now distinguishes:
     - the current mismatch where `PluginPopupScreen.tsx` still consumes `AdminConsoleSnapshot`
     - the dedicated popup snapshot contract already available at `GET /api/phase3/extension-popup`
     - the extension/background/native-message boundary for quick actions and pick-mode behavior
   - Concrete future interface design was added for:
     - `GET /api/phase3/extension-popup`
     - `EXT_PAGE_SUMMARY_GET` / `PAGE_SUMMARY_GET`
     - `PAGE_HIGHLIGHT`
     - `EXT_EXECUTION_START` / `EXECUTION_START`
     - `EXECUTION_STATUS_GET`
     - `ENVIRONMENTS_GET`
     - `DATASOURCES_GET`
34. 2026-04-20 Cross-screen consistency pass executed
   - Ran a consistency review across the generated `docs/phase3/interface/*` packages.
   - Checked screen positioning, cross-screen navigation descriptions, interface ownership boundaries, and current-vs-future implementation labeling.
   - Corrected one wording inconsistency in `docs/phase3/interface/plugin/interface-spec.md` so the current screen clearly reads from `GET /api/phase3/admin-console` today while still documenting `GET /api/phase3/extension-popup` as the dedicated future popup contract.
   - No new major cross-package contract conflicts were found beyond already recorded review items such as:
     - `execution -> monitor` still lacks explicit `runId` handoff
     - `plugin` still uses `AdminConsoleSnapshot` instead of `ExtensionPopupSnapshot`
35. 2026-04-20 Second-pass terminology normalization completed
   - Performed a second-pass normalization across current implemented cross-screen handoff wording.
   - Updated the generated docs so currently implemented shared `App.tsx` state transitions are described as `App-level handoff` / `App-level focus handoff` / `App-level selected-run handoff` instead of generic `route-state` wording.
   - Kept `route-state` wording only where the document is describing future navigation design rather than current implementation.
   - Updated files:
     - `docs/phase3/interface/docParse/functional-spec.md`
     - `docs/phase3/interface/docParse/interface-spec.md`
     - `docs/phase3/interface/aiGenerate/functional-spec.md`
     - `docs/phase3/interface/aiGenerate/interface-spec.md`
     - `docs/phase3/interface/reports/interface-spec.md`
     - `docs/phase3/interface/reportDetail/functional-spec.md`
     - `docs/phase3/interface/reportDetail/interface-spec.md`
     - `docs/phase3/interface/dataDiff/interface-spec.md`
36. 2026-04-20 Third-pass payload and naming normalization completed
   - Performed a third-pass cleanup on summary-field wording and run identifier terminology.
   - Normalized remaining interface-summary bullets toward `current direct read source` / `current direct write source` wording where needed.
   - Added identifier notes to the run-centric interface documents so they explicitly distinguish:
     - current UI selection by `runName`
     - future backend path parameters by canonical `{runId}`
   - Updated files:
     - `docs/phase3/interface/reports/interface-spec.md`
     - `docs/phase3/interface/reportDetail/interface-spec.md`
     - `docs/phase3/interface/dataDiff/interface-spec.md`
     - `docs/phase3/interface/execution/interface-spec.md`
     - `docs/phase3/interface/projects/interface-spec.md`
37. 2026-04-20 UI control/interface overview file generated
   - Generated a root-level overview file under `docs/phase3/interface/ui-control-interface-overview.md`.
   - The overview consolidates all documented screens, button-like controls, dropdown/select controls, and clickable row/card controls into one review file.
   - Each entry records:
     - current behavior
     - current request or App-level handoff
     - future interface design when the control is visible but not yet wired
   - The file covers:
     - dashboard
     - projects
     - cases
     - docParse
     - aiGenerate
     - execution
     - monitor
     - reports
     - reportDetail
     - dataDiff
     - dataTemplates
     - models
     - environments
     - plugin
38. 2026-04-20 Interface review backlog consolidated
   - Generated `docs/phase3/interface/review-backlog.md` as the implementation-ready backlog companion to the per-screen screen-doc packages.
   - The backlog consolidates current documented gaps into prioritized buckets:
     - P0 runtime chain and plugin popup contract issues
     - P1 report-chain, AI-generation, template-registry, and connection-test gaps
     - P2 dashboard/projects/cases/docParse wiring gaps
   - Each backlog item now records:
     - affected screens
     - current gap
     - required interfaces or handoff contracts
     - acceptance target
   - The backlog also records:
     - cross-screen dependency notes
     - a suggested implementation order for the next approved build phase
   - No backend or UI code was changed; this remains documentation-only work.
39. 2026-04-20 P0-1 execution→monitor runId handoff implemented (revised)
   - Phase 3 interface联调正式开始，文档-only限制解除。
   - App.tsx 新增 `selectedMonitorRunId` 状态和 `openMonitor(runId)` helper。
   - `openMonitor` 总是显式更新 selectedMonitorRunId：truthy runId 设为该值，falsy 设为 null。不保留旧 run 上下文，避免状态泄漏。
   - ExecutionScreen 的 `onOpenMonitor` 改为 `() => openMonitor(launchForm.runId)`：带 run 上下文进入 monitor。
   - MonitorScreen 新增 `selectedRunId` prop，替代之前硬编码的 `"run_8f2a1c3e"`。
   - 当 selectedRunId 为 null 时，monitor 显示"(no run selected)" + idle badge，不做虚假 fallback。
   - sidebar 直接点击 monitor 走 `handleScreenChange("monitor")`，该函数现在会显式 `setSelectedMonitorRunId(null)` 清空旧上下文 = 通用入口语义。
   - 设计决策：sidebar 入 monitor = 无选中 run 的通用入口，不复用上一次 execution 的 run。如果将来产品需要"保持上下文"行为，需要显式确认。
   - 不再有不可靠的 workQueue title 解析 fallback，canonical runId 来源干净。
   - 修改文件：
     - `ui/admin-console/src/App.tsx`
     - `ui/admin-console/src/screens/MonitorScreen.tsx`
   - 验证：build 通过，测试 11/13 通过（2 个失败为 pre-existing）。
   - 已知限制：monitor 内部运行时数据（steps、log、live page、progress）仍为占位数据，需 P0-2 落地真实 runtime API 后替换。
40. 2026-04-20 P0-2 monitor runtime API implementation completed
   - 后端新增 `RunStatusService`，基于 scheduler-requests.json + scheduler-events.json 派生 run 状态。
   - 新增 6 个 REST 端点：
     - `GET /api/phase3/runs/{runId}/status` — 运行状态、进度、计数器、控制能力
     - `GET /api/phase3/runs/{runId}/steps` — 步骤时间线（真实 STEP_* 事件或占位步骤）
     - `GET /api/phase3/runs/{runId}/runtime-log` — 运行时日志（非 step 事件）
     - `GET /api/phase3/runs/{runId}/live-page` — 当前页面状态
     - `POST /api/phase3/runs/{runId}/pause` — 写入 PAUSED 事件，验证是否可暂停
     - `POST /api/phase3/runs/{runId}/abort` — 写入 ABORTED 事件，验证是否已终止
   - pause/abort 有真实语义：校验当前状态，拒绝对终态 run 的操作，写入真实 scheduler event。
   - 前端 MonitorScreen 完全重写：
     - 不再硬编码任何运行时数据
     - selectedRunId 为 null → 显示 idle 空状态，不发请求
     - selectedRunId 存在 → 并行 fetch 4 个 read 端点，管理 loading/error/loaded 状态
     - Pause/Abort 按钮绑定真实接口，操作成功后自动刷新 monitor 数据和全局 snapshot
     - 新增 apiBaseUrl prop，由 App.tsx 传入
   - 新增 TypeScript 类型：RunStatus, RunStep, RunStepsResponse, RuntimeLogEntry, RuntimeLogResponse, LivePage, RunControlResponse
   - 修改文件：
     - 新建：`apps/local-admin-api/src/main/java/com/example/webtest/admin/service/RunStatusService.java`
     - 修改：`LocalAdminApiServer.java`（新增 handleRunEndpoint 路由）
     - 修改：`LocalAdminApiApp.java`（注入 RunStatusService）
     - 修改：`LocalAdminApiServerTest.java`（更新构造器调用）
     - 修改：`ui/admin-console/src/App.tsx`（传递 apiBaseUrl / onPauseRun / onAbortRun）
     - 修改：`ui/admin-console/src/screens/MonitorScreen.tsx`（完全重写）
     - 修改：`ui/admin-console/src/types.ts`（新增 monitor 类型）
   - 验证：
     - 前端 build 通过，测试 11/13 通过（2 个失败为 pre-existing）
     - 后端 Maven compile + test 通过
     - 真实端到端测试：启动 local-admin-api → curl 6 个端点 → 全部返回预期结构 → pause 后 status 正确变化
   - 已知限制：
     - 当 scheduler 中无 STEP_* 类型事件时，steps 返回占位步骤（来源于后端，不再由前端生成）
     - live-page 目前无真实截图，返回 screenshotPath: null
     - counters（assertions, AI calls, heals）需要运行引擎产生对应类型事件才有真实数据
     - 这些限制是"无真实运行引擎产生事件"的固有问题，不是接口设计缺口
41. 2026-04-20 P0-2 runtime API implementation review held for fixes
   - Reviewed the latest P0-2 backend/frontend runtime monitor implementation and did not approve it yet.
   - Current implementation direction is correct overall:
     - runtime read endpoints exist
     - monitor no longer relies on hardcoded runtime data
     - pause/abort buttons are wired to backend control endpoints
   - Blocking review findings recorded:
     - `pause` / `abort` endpoints currently always return HTTP 202, so the frontend treats rejected control operations as success
     - `RunStatusService.abortRun()` still allows terminal runs (`OK` / `SUCCESS` / `FAILED` / `ERROR`) to be aborted, which can incorrectly append an `ABORTED` event to a finished run
   - Remaining work before approval:
     - fix backend control-result semantics
     - fix frontend control-response handling if needed
     - add tests for rejected control actions and terminal-run abort rejection
   - No code was changed in this review pass; this was a review/recording step only.
42. 2026-04-21 P0-2 runtime API fixes reviewed and approved
   - Re-read all `docs/phase3` document folders before the final review:
     - `docs/phase3/main`
     - `docs/phase3/interface`
   - Confirmed the current project stage is Phase 3 interface integration, driven by `docs/phase3/interface/review-backlog.md`.
   - Confirmed P0-2 monitor runtime API is now approved after the blocking control-flow fixes.
   - Backend control endpoint semantics are now correct:
     - accepted pause/abort operations return HTTP 202 with `status = ACCEPTED`
     - rejected pause/abort operations return HTTP 409 with `status = REJECTED` or `ALREADY_*`
   - `RunStatusService.abortRun()` now rejects terminal run statuses instead of appending an invalid `ABORTED` event.
   - Terminal abort test coverage was strengthened to cover:
     - `OK`
     - `SUCCESS`
     - `FAILED`
     - `ERROR`
   - Frontend monitor control handlers now treat non-accepted control responses as rejected and surface the response message instead of success.
   - Verification:
     - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -am test` passed
     - `npm run build` in `ui/admin-console` passed
     - `npm test -- --run` in `ui/admin-console` still has the same 2 known pre-existing failures unrelated to P0-2
   - Next implementation prompt should start P0-3:
     - replace plugin/admin-console snapshot dependency with the dedicated popup contract `GET /api/phase3/extension-popup`
     - keep the Edge plugin as a lightweight assistive UI, not a platform business-management page
43. 2026-04-21 P0-3 plugin popup contract split completed
   - PluginPopupScreen no longer depends on AdminConsoleSnapshot.
   - New TypeScript type `ExtensionPopupSnapshot` added to `types.ts`, matching the Java `ExtensionPopupSnapshot` record.
   - PluginPopupScreen now:
     - accepts `apiBaseUrl` + `title` + `locale` props (no more `snapshot`)
     - fetches `GET /api/phase3/extension-popup` on mount
     - renders loading / error / connected host state
     - uses popup snapshot `page` fields for current-page section (title, url, domain)
     - uses popup snapshot `runtime` fields for active-run section (mode, queueState, auditState, nextAction)
     - detects running state from `runtime.queueState` instead of loosely reading `workQueue[0]`
   - App.tsx updated to pass `apiBaseUrl` instead of `snapshot` for the plugin screen.
   - No backend changes were needed — `GET /api/phase3/extension-popup` already existed.
   - Verification:
     - `npm run build` in `ui/admin-console` passed
     - `npm test -- --run` in `ui/admin-console`: 11/13 passed; command still exits non-zero because of 2 pre-existing failures
     - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -am test` passed
   - Modified files:
     - `ui/admin-console/src/types.ts`
     - `ui/admin-console/src/screens/PluginPopupScreen.tsx`
     - `ui/admin-console/src/App.tsx`
   - Remaining plugin limitations:
     - quick actions, pick mode, and locator candidates remain display-only demo content
     - no real extension/native-message wiring yet (that is future work, not P0 scope)
     - page section still shows mock forms/buttons count badge — real content-script integration is post-P0
44. 2026-04-21 P1-1 aiGenerate generation flow completed
   - Complete real AI generation chain: docParse → aiGenerate → cases.
   - Backend: new `AgentGenerateService` with three methods:
     - `generateCase(body)` → returns generatedCases, reasoning, selectedDsl, stateMachine, flowTree (deterministic mock shaped by input context)
     - `validateDsl(body)` → checks empty DSL, missing case block; returns VALID/INVALID with errors/warnings
     - `dryRun(body)` → validates DSL first, then returns runtime checks (restorePlanRef, comparisonPlanRef, environmentAccess)
   - Three new endpoints registered (dry-run before generate-case due to HttpServer prefix matching):
     - `POST /api/phase3/agent/generate-case/dry-run`
     - `POST /api/phase3/agent/generate-case`
     - `POST /api/phase3/cases/dsl/validate`
   - Reuses existing `POST /api/phase3/catalog/case` for final save-as-case.
   - Frontend types added: GenerateCaseRequest, GeneratedCaseCandidate, GenerateCaseResponse, DslValidateRequest, DslValidateResponse, DryRunRequest, DryRunResponse.
   - AiGenerateScreen.tsx fully rewritten:
     - Accepts `apiBaseUrl` and `onSaveSuccess` props
     - doGenerate → POST generate-case; doValidate → POST validate; doDryRun → validate then POST dry-run; doSave → validate then POST catalog/case then onSaveSuccess
     - Auto-generate on mount when focus exists
     - Renders real state machine, flow tree, DSL from generation result
     - Shows dry-run result inline in DSL panel
     - All buttons disabled during pending states
   - AiGenerateFocus type exported from AiGenerateScreen.tsx; duplicate removed from App.tsx.
   - App.tsx updated to pass `apiBaseUrl={apiBaseUrl}` and `onSaveSuccess={loadSnapshot}` to AiGenerateScreen.
   - Review hardening:
     - save-as-case now sends generated `dsl`, `sourceDocumentId`, and `generationMeta`
     - `CatalogPersistenceService.upsertCase()` preserves those generated draft fields in `config/phase3/project-catalog.json`
   - Modified files:
     - New: `apps/local-admin-api/src/main/java/com/example/webtest/admin/service/AgentGenerateService.java`
     - Modified: `LocalAdminApiServer.java`, `LocalAdminApiApp.java`, `CatalogPersistenceService.java`, `LocalAdminApiServerTest.java`
     - Modified: `ui/admin-console/src/types.ts`, `ui/admin-console/src/screens/AiGenerateScreen.tsx`, `ui/admin-console/src/App.tsx`
   - Verification:
     - Frontend build passed
     - Frontend tests: 11/13 passed; command still exits non-zero because of 2 pre-existing failures unrelated to P1-1
     - Maven local-admin-api test command passed
   - AI role boundary: AI only generates/explains/suggests — not acts as executor. Generated results are reviewable drafts before saving.
45. 2026-04-21 P1-2 report pages read real report artifacts completed
   - Reports / reportDetail / dataDiff now consume backend report artifact APIs instead of front-end synthesized summaries.
   - Backend: new `ReportArtifactService` reads real `runs/*/report.json` and derives:
     - `GET /api/phase3/runs/` — run list with real step/assertion counts and duration from report.json
     - `GET /api/phase3/runs/{runId}/report` — full report with steps, assertions, artifacts
     - `GET /api/phase3/runs/{runId}/data-diff` — data diff (reads data-diff.json if present, else returns mock shaped by report status)
     - `GET /api/phase3/runs/{runId}/assertions` — assertion steps extracted from report.json
     - `GET /api/phase3/runs/{runId}/artifacts` — file listing from run directory
   - `handleRunEndpoint` extended to dispatch new actions: report, data-diff, assertions, artifacts
   - Also handles `GET /api/phase3/runs/` (list) and `GET /api/phase3/runs/{runId}` (direct report) when segments < 6
   - Frontend changes:
     - New types: RunSummaryItem, RunListResponse, RunReportStep, RunReportAssertion, RunReportArtifact, RunReport, DataDiffRow, DataDiffResponse
     - ReportsScreen: fetches `/api/phase3/runs/`, maps API items to ReportViewModel for rendering, falls back to snapshot-derived buildReportViewModels when API unavailable
     - ReportDetailScreen: fetches `/api/phase3/runs/{runId}/report`, uses real steps/assertions/artifacts, falls back to selectReportViewModel
     - DataDiffScreen: fetches `/api/phase3/runs/{runId}/data-diff`, uses real diff rows/summary, falls back to synthetic makeDiffRows
     - All three screens validate API response shape before accepting (prevents misinterpretation of unrelated JSON)
   - App.tsx passes `apiBaseUrl` to all three report screens
   - Modified files:
     - New: `apps/local-admin-api/src/main/java/com/example/webtest/admin/service/ReportArtifactService.java`
     - Modified: `LocalAdminApiServer.java`, `LocalAdminApiApp.java`, `LocalAdminApiServerTest.java`
     - Modified: `ui/admin-console/src/types.ts`, `ReportsScreen.tsx`, `ReportDetailScreen.tsx`, `DataDiffScreen.tsx`, `App.tsx`
   - Review hardening (2026-04-25):
     - Fixed path traversal vulnerability in `resolveRunDir()`: added `.normalize()` + `.startsWith(root)` check to prevent `../../` escape from `reportRoot`
     - Added dedicated P1-2 endpoint test `reportArtifactEndpointsReadRealRunFiles` covering all 5 report artifact endpoints + fallback report
   - Verification:
     - Frontend build passed
     - Frontend tests: 11/13 passed (2 pre-existing failures unrelated to P1-2)
     - Maven tests: 8/8 passed (including new P1-2 endpoint test)
46. 2026-04-25 P1-2 review follow-up fix
   - Review 发现一个真实导航边界问题：
     - `reportDetail` 和 `dataDiff` 都在 sidebar 中可直接进入
     - 但当没有选中 run 时，P1-2 新实现没有统一处理空上下文
   - 修复内容：
     - `ReportDetailScreen.tsx`：`selectedRunName == null` 时不再停在 loading，而是显示明确的 `No run selected` 空状态
     - `DataDiffScreen.tsx`：`selectedRunName == null` 时不再通过 `selectReportViewModel(snapshot, null)` 错误回退到第一条报告，而是显示明确空状态
     - `DataDiffScreen.tsx`：fallback 到 snapshot synthetic diff 的条件收紧为 `fetchFailed && selectedRunName`，只在“有明确 run 且 API 失败”时启用兜底
   - 影响：
     - sidebar 直接进 `reportDetail` / `dataDiff` 的语义现在正确，不会误展示第一条 run 的内容
     - 这是一条 review-harden fix，不改变 P1-2 的主接口设计
   - 验证：
     - `npm run build` in `ui/admin-console` passed
     - `npm test -- --run` in `ui/admin-console` 仍是同样 2 个 pre-existing 失败，与本次 fix 无关
     - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test` passed
     - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -am test` 仍被既有 `report-engine` 失败阻塞，与本次 fix 无关
47. 2026-04-25 P1-3 data-template registry completed
   - Replaced front-end seeded `dataTemplates` flow with a backend-backed registry shared by `execution` and `dataTemplates`.
   - Backend:
     - new `DataTemplatePersistenceService`
     - file-backed store at `config/phase3/data-templates.json`
     - endpoints added:
       - `GET /api/phase3/data-templates`
       - `GET /api/phase3/data-templates/{templateId}`
       - `POST /api/phase3/data-templates`
       - `PUT /api/phase3/data-templates/{templateId}`
       - `DELETE /api/phase3/data-templates/{templateId}`
       - `POST /api/phase3/data-templates/import/preview`
       - `POST /api/phase3/data-templates/import/commit`
       - `POST /api/phase3/data-templates/{templateId}/dry-run`
   - Frontend:
     - `ExecutionScreen.tsx` now fetches compare-template options from the registry API
     - `DataTemplatesScreen.tsx` now fetches the same registry and supports create/edit/delete/import preview/import commit/dry-run
     - empty backend lists are now treated as valid responses instead of silently falling back to seeded constants
   - Review hardening:
     - fixed missing `apiBaseUrl` wiring from `App.tsx` into `ExecutionScreen` and `DataTemplatesScreen`
     - added admin snapshot response shape validation so unrelated JSON cannot be written into shell state
     - refreshed `App.test.tsx` for the new fetch sequence and current UI interaction model
   - Verification:
     - `npm run build` in `ui/admin-console` passed
     - `npm test -- --run` in `ui/admin-console` passed (`13/13`)
     - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test` passed
   - Remaining known repo-wide issue:
     - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -am test` still not used as release gate because of the pre-existing unrelated `report-engine` failure
   - Next recommended phase task:
     - P1-4 real connection validation for `models` and `environments`
48. 2026-04-25 P1-4 real connection validation completed
   - `models` and `environments` no longer use `setTimeout` local-only field presence checks for `Test connection`.
   - Backend added deterministic validation service:
     - new `ConnectionValidationService`
     - `POST /api/phase3/config/model/test-connection`
     - `POST /api/phase3/datasources/test-connection`
   - Validation stays within Phase 3 boundary:
     - no real outbound model-provider call
     - no real JDBC connection
     - structured mock-realistic validation only
   - Model validation now checks:
     - provider name
     - model id shape
     - endpoint format
     - timeout range
     - apiKey missing / placeholder
     - role / status legality
   - Datasource validation now checks:
     - datasource type
     - JDBC URL shape
     - driver / type match
     - schema identifier
     - username / password presence
     - mybatisEnv legality
   - Frontend `App.tsx` now posts both test actions to the real backend endpoints.
   - Frontend added basic response shape validation before accepting the result payload.
   - API unavailable behavior changed:
     - UI no longer reports fake success
     - warning-state fallback shows local-only draft completeness checks for operator reference
   - Shared UI status surface upgraded:
     - `MutationState` now supports `warning`
     - `MutationStatus` can render structured checks / warnings / latency / resolved model / resolved driver
   - Verification completed successfully:
     - `npm run build` in `ui/admin-console`
     - `npm test -- --run` in `ui/admin-console`
     - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
   - Current known limit:
     - validation remains deterministic simulation and is not a real connectivity probe by design for Phase 3
49. 2026-04-25 P1-4 docs baseline synchronized
   - Follow-up review found a state mismatch:
     - `01_dev_progress.md` and `memory.txt` marked P1-4 complete
     - several `docs/phase3/interface/*` baseline docs still said model/datasource connection tests were local-only
   - Synced the docs baseline to current implementation for:
     - `docs/phase3/interface/ui-control-interface-overview.md`
     - `docs/phase3/interface/models/functional-spec.md`
     - `docs/phase3/interface/models/interface-spec.md`
     - `docs/phase3/interface/environments/functional-spec.md`
     - `docs/phase3/interface/environments/interface-spec.md`
   - New doc state now consistently says:
     - model test connection is implemented via `POST /api/phase3/config/model/test-connection`
     - datasource test connection is implemented via `POST /api/phase3/datasources/test-connection`
     - both remain deterministic validation only and do not perform real external connectivity in Phase 3
   - This sync unblocks review of the completed P1-4 implementation without changing the code path itself.
50. 2026-04-25 P2-1 dashboard control wiring completed
   - Dashboard controls that were visible but not wired are now connected through the existing `App.tsx` screen-state model.
   - Dashboard actions completed:
     - `Refresh` now reuses `GET /api/phase3/admin-console`
     - `New run` now hands off to `execution`
     - recent-run rows now hand off to `reportDetail`
     - attention items now hand off to `reportDetail` / `monitor` / `dataDiff` / `models`
     - AI provider chips now hand off to `models`
   - To keep report-detail handoff canonical, dashboard snapshot rows now expose `runId`:
     - `AdminConsoleSnapshot.ReportRow` updated with `runId`
     - `Phase3MockDataService` now includes `runId` in mock snapshot report rows
   - No new dashboard-specific route system was added.
   - No Phase 4 typed route payload mechanism was introduced.
   - `DashboardScreen.tsx` was rewritten from demo-only controls to snapshot-driven UI callbacks.
   - Frontend tests were added for:
     - refresh reloading admin snapshot
     - new-run handoff to execution
     - recent-run canonical report-detail handoff
     - attention-item handoff for all target kinds
     - AI provider chip handoff
   - Verification passed:
     - `npm test -- --run` in `ui/admin-console`
     - `npm run build` in `ui/admin-console`
     - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
   - Known Phase 3 limit retained:
     - handoff is screen-level only; provider chips do not yet deep-focus a specific provider editor
   - Recommended next backlog item:
     - `P2-2 Complete projects import and navigation actions`
51. 2026-04-25 P2-1 dashboard docs baseline synchronized
   - Follow-up review found that P2-1 was marked complete in progress logs, but dashboard docs still described the controls as unwired.
   - Synced these docs to current implementation:
     - `docs/phase3/interface/ui-control-interface-overview.md`
     - `docs/phase3/interface/dashboard/functional-spec.md`
     - `docs/phase3/interface/dashboard/interface-spec.md`
   - Updated dashboard docs so they now consistently state:
     - `Refresh` is implemented and reuses `GET /api/phase3/admin-console`
     - `New run` is implemented as App-level handoff to `execution`
     - recent-run rows are implemented as App-level handoff to `reportDetail`
     - attention items are implemented as App-level handoff by target type
     - AI provider chips are implemented as App-level handoff to `models`
   - Updated dashboard interface contract so `reports[]` explicitly documents canonical `runId`
   - Removed stale wording such as `visual only`, `not wired`, and similar future-state language from already implemented dashboard controls
   - This follow-up changed docs only; no code path changed
52. 2026-04-25 P2-2 projects import and navigation actions completed
   - `projects` visible-but-unwired controls are now connected inside the existing `App.tsx` screen-state model.
   - Backend:
     - added `POST /api/phase3/catalog/project/import/preview`
     - added `POST /api/phase3/catalog/project/import/commit`
     - `CatalogPersistenceService` now supports deterministic import preview, duplicate/conflict detection, and file-backed commit into the project catalog
     - `LocalAdminApiServerTest` now covers the project import preview/commit flow
   - Frontend:
     - `ProjectsScreen.tsx` now wires `Import` to preview/commit
     - `New project` now reuses the existing add-row draft flow and persists through `POST /api/phase3/catalog/project`
     - project `Reports` / `View reports` now hand off to `reports`
     - `Enter project` now hands off to `cases`
     - `App.tsx` now keeps `selectedCaseProjectKey`, `selectedReportsProjectKey`, and project import preview/state
     - `CasesScreen.tsx` and `ReportsScreen.tsx` accept `initialProjectKey` to reuse existing screen-state instead of adding a router
   - Hardening:
     - `ReportsScreen.tsx` now falls back to snapshot reports when `GET /api/phase3/runs/` returns an empty list
     - `ProjectsScreen.tsx` draft editor rows now use stable index-based React keys so editing `project.key` no longer remounts the row
   - Verification passed:
     - `npm test -- --run` in `ui/admin-console` (`20/20`)
     - `npm run build` in `ui/admin-console`
     - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test` (`11/11`)
   - Current limit:
     - project navigation remains App-level handoff only; no real route layer or typed route payload was introduced
   - Review note:
     - if review finds stale `projects` wording in `docs/phase3/interface`, sync docs before considering the step fully approved
53. 2026-04-25 P2-2 projects docs baseline synchronized
   - Follow-up review found that P2-2 was marked complete in progress logs, but `projects` docs still described import and navigation controls as unwired or future-only.
   - Synced these docs to current implementation:
     - `docs/phase3/interface/ui-control-interface-overview.md`
     - `docs/phase3/interface/projects/functional-spec.md`
     - `docs/phase3/interface/projects/interface-spec.md`
   - Updated the docs so they now consistently state:
     - `Import` is implemented via `POST /api/phase3/catalog/project/import/preview` -> `POST /api/phase3/catalog/project/import/commit`
     - `New project` is implemented by reusing the add-row draft flow and persisting through `POST /api/phase3/catalog/project`
     - `Reports` / `View reports` are implemented as App-level handoff to `reports`
     - `Enter project` is implemented as App-level handoff to `cases`
   - Rewrote the stale “recommended / future” wording in `projects/interface-spec.md` so it now reflects the current implemented P2-2 state, while still leaving future language only for truly unfinished areas.
   - This follow-up changed docs only; no code path changed
54. 2026-04-25 P2-3 cases editor-side read/write interfaces completed
   - Scope: Wire the 7 case-detail API endpoints and make CasesScreen tabs functional with real backend data
   - Backend:
     - NEW `CaseDetailService.java`: File-backed case detail service under `config/phase3/case-details/<caseId>/`
       - GET/PUT DSL (auto-versioning), POST validate, GET/PUT state-machine, GET plans, GET history
       - Path traversal protection, default/mock data builders
     - `LocalAdminApiServer.java`: 11-param constructor chain (9→10→11), route `/api/phase3/cases/` with handleCaseDetailEndpoint dispatcher
     - PUT endpoints return 202 Accepted
   - Frontend:
     - `types.ts`: 7 new types (CaseDslResponse, CaseDslValidateResponse, CaseDslSaveResponse, CaseStateMachineResponse, CasePlansResponse, CaseHistoryResponse)
     - `CasesScreen.tsx`: Tab switching (overview/dsl/stateMachine/plans/history), API-backed tab data, DSL editor with validate/save, state machine viewer with save, plans/history panels, hero buttons wired
     - `App.tsx`: Pass apiBaseUrl to CasesScreen
   - Tests:
     - `LocalAdminApiServerTest.java`: 10-step `caseDetailEndpoints` test covering all 7 endpoints
   - Verification: npm build PASS, npm test 20/20, mvn test 12/12
55. 2026-04-26 P2-5 reportDetail controls wiring completed
   - All visible-but-unwired controls on reportDetail page are now connected.
   - Backend: ReportArtifactService extended with `getRecovery()` and `getAiDecisions()` (file-backed + mock fallback).
   - Two new cases added to `handleRunEndpoint` in LocalAdminApiServer: `recovery` and `ai-decisions`.
   - Frontend: ReportDetailScreen completely rewritten:
     - 6 tabs with `ReportDetailTab` type, on-demand API fetches per tab, cached results
     - Artifact listing drawer via `GET .../artifacts`
     - Re-run handoff via `onRerun` callback → App.tsx sets launchForm and switches to execution
   - Types added: RecoveryItem, RecoveryResponse, AiDecisionItem, AiDecisionsResponse, RunAssertionsResponse, RunArtifactsResponse
   - Tests: 3 frontend tests (tab switching, artifact listing, re-run handoff), backend recovery/ai-decisions assertions
   - Docs synced: ui-control-interface-overview.md, reportDetail/functional-spec.md, reportDetail/interface-spec.md
   - Verification: npm build PASS, npm test 26/26, mvn test 13/13
   - Remaining limits:
     - Recovery and AI decisions data are deterministic mock when no real run artifacts exist
     - Artifact listing drawer shows file paths only, no inline view/download
     - Re-run pre-fill depends on report data availability for environment/model
56. 2026-04-26 P2-4 docParse document-service actions completed
   - Scope: Wire the 3 document-service API endpoints and make DocParseScreen Upload/Re-parse/Manual edit buttons functional
   - Backend:
     - NEW `DocumentPersistenceService.java`: File-backed persistence under `config/phase3/documents/<documentId>/`
       - upload (validates projectKey/fileName, persists raw.json + parse-result.json + meta.json)
       - reparse (checks meta exists, regenerates parse-result.json)
       - saveParseResult (overwrites parse-result.json with edited cases)
       - getParseResult (reads persisted parse result)
       - Path traversal protection via .normalize() + .startsWith(root)
     - `LocalAdminApiServer.java`: 12-param constructor chain (9→12, 10→12, 11→12)
       - `/api/phase3/documents/upload` route before `/api/phase3/documents/` (prefix conflict avoidance)
       - `handleDocumentEndpoint` dispatches by path segments
   - Frontend:
     - `types.ts`: 4 new types (DocumentUploadResponse, DocumentReparseResponse, DocumentParseResultSaveResponse, DocumentParseResult)
     - `DocParseScreen.tsx`: apiBaseUrl prop, FileReader upload, Re-parse with parse-result refresh, Manual edit with JSON editor, action status bar
     - `App.tsx`: Pass apiBaseUrl to DocParseScreen
   - Tests:
     - `LocalAdminApiServerTest.java`: 7-step `documentServiceEndpoints` test covering upload, get, reparse, manual edit, error cases
   - Docs synced: ui-control-interface-overview.md, docParse/functional-spec.md, docParse/interface-spec.md
   - Verification: npm build PASS, npm test 20/20, mvn test 13/13
57. 2026-04-27 P2-5 reportDetail docs baseline synchronized
   - Follow-up review found that P2-5 code and tests were already correct, but `reportDetail` docs still contained stale future-state wording for artifact download, re-run, and tab wiring.
   - Synced these docs to current implementation:
     - `docs/phase3/interface/reportDetail/functional-spec.md`
     - `docs/phase3/interface/reportDetail/interface-spec.md`
   - Updated the docs so they now consistently state:
     - `Download artifacts` is implemented via `GET /api/phase3/runs/{runId}/artifacts` and opens an artifact listing drawer
     - `Re-run` is implemented as App-level handoff into `execution`
     - report-detail tabs are already API-backed current behavior, not future design
   - Removed stale wording such as `future artifact download flow`, `future re-run handoff`, `no actual artifact opening or download exists`, and `future true report interfaces and artifact actions`
   - Kept remaining future/limit wording only for real limits:
     - no inline artifact content open/download
     - some fields still fall back to snapshot-derived view model data
     - recovery / AI decisions may use deterministic mock data when no real run artifacts exist
   - This follow-up changed docs only; no code path changed
58. 2026-04-27 P2-6 dataDiff control wiring completed
   - Wired the two remaining visible-but-unwired controls on the dataDiff page: View raw JSON and Re-restore.
   - Backend: ReportArtifactService extended with:
     - `getRawDataDiff(runId)` — reads `data-diff-raw.json` or builds mock inline raw snapshots from existing diff rows
     - `getRestoreResult(runId)` — reads `restore-result.json` or returns mock partial restore result
     - `restoreRetry(runId, body)` — validates runId, rejects if RETRY_IN_PROGRESS, returns ACCEPTED with operator/reason
   - LocalAdminApiServer.handleRunEndpoint extended:
     - `data-diff` case now checks for `raw` sub-action
     - new `restore-result` case (GET)
     - new `restore` case with `retry` sub-action (POST, uses controlHttpStatus)
   - Frontend DataDiffScreen.tsx:
     - View raw JSON: fetches data-diff/raw, opens drawer with before/after/afterRestore tabs, shows raw JSON in `<pre>`
     - Re-restore: posts restore/retry, shows success/rejected/error status bar, refreshes diff data on success
     - Both buttons disabled during pending states; action states reset on run change
   - Types added: RawDataDiffEntry, RawDataDiffResponse, RestoreResultItem, RestoreResultResponse, RestoreRetryResponse
   - No App.tsx changes needed — existing apiBaseUrl and selectedRunName wiring was sufficient
   - Tests:
     - Backend: 3 new assertions inside reportArtifactEndpointsReadRealRunFiles (data-diff/raw, restore-result, restore/retry)
     - Frontend: 3 new tests (raw drawer open/tab/close, re-restore accepted, re-restore rejected)
   - Docs synced: ui-control-interface-overview.md, dataDiff/functional-spec.md, dataDiff/interface-spec.md
   - Verification: npm test 29/29, npm build PASS, Maven test 13/13
   - Known limits:
     - raw diff and restore-result are deterministic mock when no real artifacts exist
     - re-restore records intent only, does not trigger real restore workflow
     - raw JSON drawer does not support copy-to-clipboard or download

59. P1-3 Documentation Sync (2026-04-27)
   - All P1-3 code was already complete (2026-04-25). This session synced 5 stale interface docs.
   - Files updated:
     - ui-control-interface-overview.md (execution + dataTemplates rows)
     - dataTemplates/functional-spec.md (all controls → implemented)
     - dataTemplates/interface-spec.md (sections 2-11 fully synced)
     - execution/interface-spec.md (compare-template source → backend registry)
     - review-backlog.md (P1-3 → DONE)
   - Pattern: "visual only" / "future" / "front-end seeded" language replaced with implemented endpoint references
   - Verification: Maven 13/13 pass; npm test/build 未在本次 doc-only 变更中重跑

60. P0-1 / P0-2 Monitor RunId Handoff + Runtime APIs (2026-04-28)
   - Both P0-1 and P0-2 code was already fully implemented. This session added tests and synced docs.
   - Code already in place:
     - App.tsx: selectedMonitorRunId state, openMonitor(runId) helper, execution onOpenMonitor wired
     - MonitorScreen.tsx: idle/loading/error/loaded states, 4 runtime API fetches, Pause/Abort wiring
     - types.ts: RunStatus, RunStep, RuntimeLogEntry, LivePage, RunControlResponse
   - Tests added (App.test.tsx): 2 new tests (31 total)
     - execution → monitor runId handoff
     - monitor idle state when no runId
   - Docs synced:
     - execution/interface-spec.md, execution/functional-spec.md (runId handoff)
     - monitor/functional-spec.md, monitor/interface-spec.md (full sync from placeholder to implemented)
     - ui-control-interface-overview.md (execution + monitor sections)
     - review-backlog.md (P0-1 + P0-2 → DONE)
   - Verification: npm test 31/31, npm build PASS

61. P0-3 Plugin Popup Dedicated Contract (2026-04-28)
   - P0-3 code was already implemented. This session added tests and synced docs.
   - Code already in place:
     - PluginPopupScreen.tsx: consumes ExtensionPopupSnapshot from GET /api/phase3/extension-popup
     - Props: apiBaseUrl, title, locale (no AdminConsoleSnapshot prop)
     - Loading/loaded/error states for popup snapshot fetch
     - Page/runtime sections render from popupSnapshot.page and popupSnapshot.runtime
     - App.tsx passes apiBaseUrl to plugin screen (not snapshot)
     - Backend: Phase3MockDataService.buildExtensionPopupSnapshot()
     - Quick actions remain display-only (require extension/background/native infrastructure)
   - Tests added (App.test.tsx): 2 new tests (33 total)
     - popup loads from dedicated extension-popup endpoint
     - popup error state when extension-popup fails
   - Docs synced:
     - plugin/functional-spec.md (§5, §6.2, §6.3, §8, §9, §11, §15)
     - plugin/interface-spec.md (§2, §3, §4, §6.1, §8, §10)
     - review-backlog.md (P0-3 → DONE)
     - ui-control-interface-overview.md (§15 plugin note, §16 resolved list)
   - Verification: npm test 33/33, npm build PASS
62. P1-1 aiGenerate real generation flow baseline re-verified and docs finalized (2026-04-28)
   - This pass re-checked the current `docParse -> aiGenerate -> cases` chain instead of assuming the backlog item was still unimplemented.
   - Result of code inspection:
     - `DocParseScreen.tsx` already builds stable focus payload and hands it to `App.tsx`
     - `App.tsx` already stores `aiGenerateFocus` and switches to `aiGenerate`
     - `AiGenerateScreen.tsx` already calls:
       - `POST /api/phase3/agent/generate-case`
       - `POST /api/phase3/cases/dsl/validate`
       - `POST /api/phase3/agent/generate-case/dry-run`
       - `POST /api/phase3/catalog/case`
     - validate-first behavior for Dry-run and Save is already implemented
     - save already reuses the canonical case-catalog persistence path; do not introduce a parallel generated-case save contract
     - this pass added and re-verified frontend tests for generate, dry-run, save, and failure visibility
   - Real issue found in this pass:
     - `docs/phase3/interface/aiGenerate/interface-spec.md` still described the page as lacking backend generation/validation/persistence requests
     - `docs/phase3/interface/aiGenerate/functional-spec.md` still had a small amount of outdated future-style wording around current review flow
   - Docs updated:
     - `docs/phase3/interface/aiGenerate/interface-spec.md`
     - `docs/phase3/interface/aiGenerate/functional-spec.md`
   - Docs updated in this pass:
     - `docs/phase3/interface/review-backlog.md`
     - `docs/phase3/interface/ui-control-interface-overview.md`
       - both now explicitly reflect P1-1 as implemented instead of keeping stale future/unwired wording
   - Docs checked and already aligned, so no content change was required:
     - `docs/phase3/interface/docParse/*`
     - `docs/phase3/interface/cases/*`
   - Verification rerun on the current baseline:
     - `npm test -- --run` in `ui/admin-console`: PASS (37/37)
     - `npm run build` in `ui/admin-console`: PASS
     - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`: PASS (13/13)
   - Keep these boundaries:
     - backend generation remains deterministic mock shaped by input context
     - candidate tab switch remains local-only with one backend-selected DSL payload per generation
     - `aiGenerate` still has no automatic downstream navigation into `cases` or `execution`
- 2026-04-29: Re-opened P1-2 and tightened the canonical report artifact path.
  - `reports`, `reportDetail`, and `dataDiff` now use explicit `runId` handoff rather than `runName`-based selection.
  - `ReportsScreen` is backend-first on `GET /api/phase3/runs/`; snapshot/view-model synthesis is fallback-only.
  - `DataDiffScreen` now consumes `GET /api/phase3/runs/{runId}/restore-result` and refreshes restore-result together with diff data after `POST /api/phase3/runs/{runId}/restore/retry`.
  - Frontend tests were expanded/re-verified for report->detail->diff handoff, artifact failure tolerance, raw diff error state, and restore-result refresh.
  - Local verification exposed Vitest worker/resource instability on this machine; serialized targeted runs for the modified report flows passed, and backend `LocalAdminApiServerTest` passed.
- 2026-05-04: Completed the current highest-priority remaining plugin quick-action gap instead of falling back to monitor drill-down, because the real extension popup chain was already present.
  - Real chain confirmed before coding:
    - `extension/edge-extension/popup.js`
    - `extension/edge-extension/background.js`
    - `apps/native-host`
    - `apps/local-admin-api`
  - Added extension-specific local-admin-api endpoints:
    - `POST /api/phase3/extension/page-summary`
    - `POST /api/phase3/extension/platform-handoff`
  - Added native-host request routing for:
    - `PAGE_SUMMARY_GET`
    - `PLATFORM_HANDOFF_PREPARE`
  - Real extension popup quick actions now wired:
    - `Page summary` -> popup -> background -> native-host -> local-admin-api
    - `Open in platform` -> popup -> background -> native-host -> local-admin-api -> background tab-open -> platform `execution` App handoff
    - `Copy` -> popup-local clipboard write
    - `Use in DSL` -> popup -> background -> native-host -> local-admin-api -> background tab-open -> platform `aiGenerate` App handoff
  - `ui/admin-console/src/App.tsx` now consumes lightweight plugin query params and maps them into existing App state only:
    - `launchForm`
    - `aiGenerateFocus`
    - no new router introduced
  - Keep this architecture boundary:
    - popup collects lightweight context and triggers actions
    - background owns native-host bridge and tab open
    - native-host only dispatches to local-admin-api
    - local-admin-api owns deterministic page-summary and handoff URL shaping
    - platform App owns final screen/state handoff
  - Verification passed:
    - `npm test -- --run src/popup.test.js`
    - targeted `App.test.tsx` handoff tests
    - `npm run build` in `ui/admin-console`
    - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/native-host -Dtest=NativeHostMessageProcessorTest test`
    - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
  - Remaining plugin limits after this pass:
    - `Pick element` still not implemented
    - `Quick smoke test` still not implemented
    - page summary is deterministic and does not yet use real content-script DOM extraction
    - `Use in DSL` currently targets `aiGenerate`, not direct `cases` DSL insertion
- 2026-05-05: Completed P0-5 Plugin Pick Element Real Chain.
  - Added `extension/edge-extension/content-script.js` and registered it in `manifest.json`.
  - Real pick flow now stays fully inside the extension:
    - popup `Pick element` trigger
    - background bridge
    - content script hover/selected highlight
    - content script selected-element shaping with locator candidates
    - popup result render
  - Popup now renders real selected element fields:
    - `tag`
    - `text`
    - `id`
    - `name`
    - recommended locator
    - locator candidate list
  - `Copy` and `Use in DSL` now consume the current real recommended locator from pick result instead of the old static popup suggestion.
  - Kept the Phase 3 boundary:
    - popup = trigger/render only
    - background = bridge only
    - content script = DOM collection/highlight only
    - native-host unchanged
    - local-admin-api unchanged
    - no scheduler/report/run protocol changes
    - no complex router introduced
  - Added verification:
    - `npm test -- --run src/popup.test.js`
    - `npm test -- --run src/background.test.js`
    - `npm test -- --run src/content-script.test.js`
    - `npm run build`
  - Remaining plugin limits after this pass:
    - `Quick smoke test` still not implemented
    - locator row selection is not yet interactive local state
    - `Use in DSL` still targets `aiGenerate`, not direct `cases` DSL insertion
- 2026-05-05: Completed P0-6 Plugin Quick Smoke Test Real Chain.
  - Added a real `Quick smoke test` action to `extension/edge-extension/popup.html` + `popup.js`.
  - The action uses the current active-tab context and reuses the existing launch-form fields:
    - `runId`
    - `projectKey`
    - `owner`
    - `environment`
    - `detail`
  - Reused the existing scheduler request chain instead of adding a new popup execution protocol:
    - popup -> background `channel: "native-host"`
    - native-host `type: "SCHEDULER_REQUEST_CREATE"`
    - local-admin-api `POST /api/phase3/scheduler/requests`
  - Popup now renders deterministic quick-smoke launch state:
    - `pending`
    - `success`
    - `error`
    - `runId`
    - queue status
    - next step back to platform execution/monitor
  - Background remained on the generic native-host forwarding path; no new extension message type was required for this feature.
  - Verification passed:
    - `npm test -- --run src/popup.test.js`
    - `npm test -- --run src/background.test.js`
  - Remaining plugin limits after this pass:
    - popup mirror screen in `ui/admin-console` is still a demo surface
    - locator row selection is not yet interactive local state
    - `Use in DSL` still targets `aiGenerate`, not direct `cases` DSL insertion
- 2026-05-05: Completed P0-7 Monitor Drill-Down Panels.
  - Updated `ui/admin-console/src/screens/MonitorScreen.tsx` to add local drill-down panels for:
    - step rows
    - runtime log rows
  - Kept the implementation fully frontend-local:
    - no new route
    - no new backend endpoint
    - no run/report/scheduler protocol semantic change
  - Reused existing monitor read interfaces only:
    - `GET /api/phase3/runs/{runId}/steps`
    - `GET /api/phase3/runs/{runId}/runtime-log`
  - Step detail now renders the existing step payload fields:
    - `index`
    - `label`
    - `state`
    - `durationMs`
    - `startedAt`
    - `note`
  - Runtime-log detail now renders the existing log payload plus optional extended fields when present.
  - `selectedRunId` changes clear the previously opened drill-down state, and idle/loading/error/no-data states do not expose stale detail panels.
  - Verification passed:
    - `npm test -- --run src/screens/MonitorScreen.test.tsx`
    - `npm run build`
  - Remaining monitor limits after this pass:
    - runtime data remains deterministic mock when no real run artifacts exist
    - live page remains structured-data only
    - monitor drill-down stays local; it does not become a separate route or editor surface
- 2026-05-05: Completed P1-5 Models Routing Rule Local Edit Flow.
  - Updated `ui/admin-console/src/screens/ModelConfigScreen.tsx` so the routing-rule edit icon now opens a local editor modal instead of remaining visual-only.
  - Added editable local draft fields for:
    - `task`
    - `primary`
    - `fallback`
    - `reason`
  - `Apply` now writes the edited routing rule back into the current front-end draft only.
  - `Cancel` now closes without mutating the parent routing-rule draft.
  - Updated `ui/admin-console/src/App.tsx` to pass `setModelRoutingRules` into `ModelConfigScreen`.
  - Kept persistence on the existing model-config save path:
    - `buildModelConfigItems(modelProviders, modelRoutingRules)`
    - repeated `POST /api/phase3/config/model`
  - No backend endpoint or provider-connection semantics were changed.
  - Added tests:
    - `ui/admin-console/src/screens/ModelConfigScreen.test.tsx`
    - `ui/admin-console/src/App.test.tsx` integration coverage for save-path reuse
  - Verification passed:
    - `npm test -- --run src/screens/ModelConfigScreen.test.tsx`
    - `npm test -- --run src/App.test.tsx -t "persists locally edited routing rules through the existing model config save flow"`
    - `npm run build`
  - Remaining models limits after this pass:
    - routing-rule editing is still single-rule local modal state
    - persistence still uses generic config-item storage instead of a typed models API
- 2026-05-05: Completed P1-6 Execution Contract Hint Local Drawer.
  - Updated `ui/admin-console/src/screens/ExecutionScreen.tsx` so the header contract hint button now opens a local execution-contract help panel.
  - Kept the implementation fully frontend-local:
    - no new backend endpoint
    - no scheduler request or event semantic change
    - no new route
    - `execution` remains a run-preparation / submission page
  - The panel documents the current execution-page contract only:
    - `Run ID`
    - `Project`
    - `Owner`
    - `Environment`
    - `Target URL`
    - `Execution model`
    - `Compare data templates`
    - `Database connection`
    - `Run -> POST /api/phase3/scheduler/requests`
    - `Execution / Open Audit -> POST /api/phase3/scheduler/events`
    - prepared-case / queue / monitor handoff boundaries
  - Added `ui/admin-console/src/screens/ExecutionScreen.test.tsx` for:
    - open help panel
    - close help panel
    - form/button non-regression while panel is open
    - empty `runId` and queue/prepared-case variation tolerance
  - Verification passed:
    - `npm test -- --run src/screens/ExecutionScreen.test.tsx`
    - `npm run build`
  - Remaining execution limits after this pass:
    - the panel is descriptive only; it does not edit payloads
    - queue rows and prepared-case cards remain display-only
- 2026-05-05: Completed P1-7 Execution Queue Row Drill-Down.
  - Updated `ui/admin-console/src/screens/ExecutionScreen.tsx` so queue rows are now clickable instead of display-only.
  - Reused existing app-level handoff only:
    - queue row click -> `onOpenQueueItem(item.title)`
    - `App.tsx` derives the current run identity from queue `title`
    - `App.tsx` reuses `openMonitor(runId)`
  - Fixed the first implementation to `monitor`, not `reportDetail`.
    - Reason: current `workQueue` shape still only exposes `title`, `owner`, `state`, and `detail`
    - That shape is sufficient for a stable lightweight monitor handoff
    - It is not sufficient for reliable monitor-vs-report branching without inventing new metadata
  - Added explicit queue-row `aria-label` text for drill-down.
  - Kept the Phase 3 boundary:
    - no new backend endpoint
    - no new route system
    - no typed router payload
    - no scheduler request/event semantic change
  - Added verification:
    - `npm test -- --run src/screens/ExecutionScreen.test.tsx`
    - `npm test -- --run src/App.test.tsx -t "opens monitor from execution queue-row drill-down via the existing App handoff"`
    - `npm run build`
  - Remaining execution limits after this pass:
    - queue drill-down still depends on queue `title` format because the snapshot queue shape has no dedicated `runId`
    - prepared-case cards remain display-only
- 2026-05-05: Completed P1-8 Execution Prepared-Case Card Drill-Down.
  - Updated `ui/admin-console/src/screens/ExecutionScreen.tsx` so prepared-case cards are now clickable instead of display-only.
  - Reused existing app-level `cases` handoff only:
    - prepared-case card click -> `onOpenPreparedCase(item.id, item.projectKey)`
    - `App.tsx` stores the current cases context in:
      - `selectedCaseProjectKey`
      - `selectedCaseId`
    - `App.tsx` switches back to `cases`
    - `CasesScreen.tsx` now accepts `initialCaseId` and reopens the matching case in the existing detail canvas
  - Added explicit prepared-case-card `aria-label` text for drill-down.
  - Kept the Phase 3 boundary:
    - no new backend endpoint
    - no new route system
    - no typed router payload
    - no scheduler request/event semantic change
    - no standalone prepared-case detail panel
  - Added verification:
    - `npm test -- --run src/screens/ExecutionScreen.test.tsx`
    - `npm test -- --run src/App.test.tsx -t "opens cases from execution prepared-case drill-down via the existing App handoff"`
  - Remaining execution limits after this pass:
    - prepared-case drill-down carries only `projectKey` + `caseId`
    - the reopen path still depends on the matching case being present in the current app snapshot/draft
- 2026-05-06: Completed P2-1 Dashboard Control Wiring.
  - Re-checked the current dashboard code instead of assuming the backlog state was still accurate.
  - Confirmed these handoffs were already implemented and kept them unchanged:
    - `Refresh` -> existing shell `GET /api/phase3/admin-console` reload path
    - `New run` -> App-level handoff into `execution`
    - recent-run row -> App-level handoff into `reportDetail`
    - attention item -> App-level handoff into `reportDetail` / `monitor` / `dataDiff` / `models`
    - AI provider chip -> App-level handoff into `models`
  - Added the remaining missing UX piece:
    - explicit dashboard refresh feedback with `pending` / `success` / `error`
  - Updated `ui/admin-console/src/App.tsx` to own `dashboardRefreshState`.
  - Updated `ui/admin-console/src/screens/DashboardScreen.tsx` to:
    - disable `Refresh` while pending
    - render refresh feedback through `MutationStatus`
  - Kept the Phase 3 boundary:
    - no dashboard-specific backend endpoint
    - no route system or typed router payload
    - no execution / monitor / reportDetail protocol semantic change
  - Added verification:
    - `npm test -- --run src/App.test.tsx -t "refreshes the dashboard snapshot and hands off New run to Execution"`
    - `npm test -- --run src/App.test.tsx -t "opens dashboard recent-run rows with the canonical runId"`
    - `npm test -- --run src/App.test.tsx -t "routes dashboard attention items and provider chips through existing App handoff state"`
    - `npm run build`
  - Remaining dashboard limits after this pass:
    - metric cards remain display-only
    - attention items are still front-end derived from snapshot data
    - dashboard still does not own full first-load loading/empty/error page states
- 2026-05-06: Completed P1-4 review hardening follow-up.
  - Review found one remaining mismatch in the completed P1-4 flow:
    - the UI still downgraded backend validation failure into warning-state local fallback output
  - Fixed `ui/admin-console/src/App.tsx` so:
    - model and datasource test-connection no longer synthesize local fallback validation payloads
    - transport / malformed / non-2xx validation failures now surface as explicit error state
    - backend validation remains the only authoritative validation result source
  - Added frontend regression coverage in `ui/admin-console/src/App.test.tsx` for:
    - models pending / success / error
    - environments pending / success / error
    - save/test isolation on both screens
  - Extended backend endpoint coverage in `LocalAdminApiServerTest.java` with warning-path assertions for:
    - `POST /api/phase3/config/model/test-connection`
    - `POST /api/phase3/datasources/test-connection`
  - Synced Phase 3 docs and backlog so they no longer describe local fallback validation as current behavior.
  - Verification passed:
    - `npm test -- --run src/App.test.tsx -t "shows pending, success, and error states for model connection validation|keeps model save and test connection state isolated|shows pending, success, and error states for datasource connection validation|keeps environment save and test connection state isolated"`
    - `npm run build`
    - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
  - Attempted but not used as gate:
    - `npm test -- --run src/App.test.tsx` timed out in the current environment
- 2026-05-06: Completed P2-2 Projects Import And Navigation Actions.
  - Re-checked the current `projects` implementation before changing code.
  - Confirmed the intended Phase 3 boundary was already respected in code:
    - `Import` uses:
      - `POST /api/phase3/catalog/project/import/preview`
      - `POST /api/phase3/catalog/project/import/commit`
    - `New project` reuses add-row draft flow and persists through:
      - `POST /api/phase3/catalog/project`
    - `Enter project` reuses existing App-level handoff into `cases`
    - `View reports` and card `Reports` reuse existing App-level handoff into `reports`
  - Added focused frontend regression coverage for import error feedback so `pending / success / error` is explicitly covered.
  - Synced docs to the real implemented import boundary:
    - deterministic JSON review only
    - no CSV parser
    - no file-upload wizard
  - Docs synced:
    - `docs/phase3/interface/projects/functional-spec.md`
    - `docs/phase3/interface/projects/interface-spec.md`
    - `docs/phase3/interface/ui-control-interface-overview.md`
    - `docs/phase3/interface/review-backlog.md`
  - Verification for this pass:
    - `npm test -- --run src/App.test.tsx -t "runs project import preview and commit through the new catalog import endpoints"`: PASS
    - `npm test -- --run src/App.test.tsx -t "shows project import error feedback when preview fails"`: PASS
    - `npm test -- --run src/App.test.tsx -t "adds a new project row and persists it through the existing catalog save flow"`: PASS
    - `npm test -- --run src/App.test.tsx -t "hands off Enter project into Cases with the selected project context"`: PASS
    - `npm run build` in `ui/admin-console`: PASS
  - Attempted but not used as gate:
    - aggregated `App.test.tsx` pattern run timed out / OOMed in the current environment
    - isolated `projects -> reports` handoff test selection was unstable under current Vitest worker/filter behavior on this machine
  - Keep this boundary:
    - no new route system
    - no new projects-specific handoff payload
    - no new project-detail protocol
    - no cases/reports handoff semantic change
- 2026-05-06: Completed P2-4 DocParse read-side completion follow-up.
  - Re-checked the real `docParse` gap instead of trusting the older P2-4 wording.
  - Confirmed write-side P2-4 was already present:
    - `POST /api/phase3/documents/upload`
    - `POST /api/phase3/documents/{documentId}/reparse`
    - `PUT /api/phase3/documents/{documentId}/parse-result`
  - Added the remaining read-side document interfaces in `local-admin-api`:
    - `GET /api/phase3/documents/{documentId}/raw`
    - `GET /api/phase3/documents/{documentId}/versions`
  - `DocumentPersistenceService` now also records lightweight version-history entries for:
    - upload
    - re-parse
    - manual edit
  - Reworked `ui/admin-console/src/screens/DocParseScreen.tsx` so opening a document now attempts backend hydration for:
    - parse result
    - raw document
    - version history
  - `Raw document` and `Version history` now expose explicit:
    - loading
    - empty
    - error
    states
  - `Re-parse` and `Manual edit` now refresh parse/raw/version detail together after success.
  - Kept the current Phase 3 boundary:
    - document catalog is still synthetic shell data from the admin snapshot
    - parse detail still keeps synthetic fallback when no persisted backend document exists yet
    - version history remains lightweight event metadata only
  - Docs synced:
    - `docs/phase3/interface/docParse/functional-spec.md`
    - `docs/phase3/interface/docParse/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
    - `docs/phase3/interface/ui-control-interface-overview.md`
  - Verification:
    - `mvn "-Dmaven.repo.local=.m2/repository" -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest#documentServiceEndpoints test`: PASS
    - `npm run build` in `ui/admin-console`: PASS
  - Attempted but not used as gate:
    - targeted Vitest runs for `App.test.tsx` and `DocParseScreen.test.tsx` timed out in the current environment
- 2026-05-06: Completed the small P2-4 follow-up for DocParse copy cleanup and session-catalog boundary clarification.
  - Cleaned `ui/admin-console/src/screens/DocParseScreen.tsx` so the screen no longer contains garbled UI copy.
  - Kept scope intentionally small:
    - no `GET /api/phase3/documents`
    - no backend catalog contract change
  - Added a small frontend-only guard:
    - synthetic document rows from `buildDocuments(snapshot)` are merged with session-local uploaded rows
    - uploaded rows therefore survive snapshot rebuilds in the same frontend session
  - Keep the explicit boundary:
    - uploaded rows still disappear after remount / fresh app load because the catalog is still synthetic and session-local
  - Synced this limitation into:
    - `docs/phase3/interface/docParse/functional-spec.md`
    - `docs/phase3/interface/docParse/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
    - `01_dev_progress.md`
  - Verification:
    - `npm run build` in `ui/admin-console`: PASS
- 2026-05-06: Completed the current `P2-3 cases` follow-up within the existing Phase 3 boundary.
  - Re-checked the real implementation first instead of trusting the stale backlog text.
  - Confirmed the backend case-detail chain was already implemented:
    - `GET /api/phase3/cases/{caseId}/dsl`
    - `POST /api/phase3/cases/{caseId}/dsl/validate`
    - `PUT /api/phase3/cases/{caseId}/dsl`
    - `GET /api/phase3/cases/{caseId}/state-machine`
    - `PUT /api/phase3/cases/{caseId}/state-machine`
    - `GET /api/phase3/cases/{caseId}/plans`
    - `GET /api/phase3/cases/{caseId}/history`
  - Rewrote `ui/admin-console/src/screens/CasesScreen.tsx` because the prior screen file had both weak UX state handling and heavy text corruption.
  - Kept the same Phase 3 boundary and tightened the frontend behavior only:
    - explicit loading / empty / error surfacing for backend-backed tabs
    - explicit mutation feedback for DSL validate/save and state-machine save
    - case switch resets active tab to `overview` and clears stale tab-specific data
  - Added focused frontend coverage in `ui/admin-console/src/screens/CasesScreen.test.tsx` for:
    - DSL load / validate / save
    - invalid JSON validation feedback
    - plans/history state surfacing
    - state-machine save feedback
    - case-switch reset behavior
  - Synced stale docs so `P2-3` is now marked DONE in:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Verification:
    - `npm run build` in `ui/admin-console`: PASS
  - Attempted but not used as gate:
    - `npm test -- --run src/screens/CasesScreen.test.tsx`
    - `npx vitest run src/screens/CasesScreen.test.tsx --pool=forks --poolOptions.forks.singleFork --reporter=verbose`
    - current machine still shows Vitest timeout / worker startup instability, so build is the stable verification result
- 2026-05-06: Completed the current `CasesScreen` review-fix follow-up.
  - Added real in-flight request isolation to `ui/admin-console/src/screens/CasesScreen.tsx`:
    - per-tab `AbortController`
    - per-tab request-version guards
    - old case/tab responses can no longer overwrite the currently opened case detail
  - Applied `encodeURIComponent(caseId)` to all case-detail fetch URLs:
    - DSL read / validate / save
    - state-machine read / save
    - plans read
    - history read
  - Extended `ui/admin-console/src/screens/CasesScreen.test.tsx` with focused coverage for:
    - stale response race after switching case
    - encoded caseId in backend detail URL
  - Verification:
    - `npm run build` in `ui/admin-console`: PASS
  - Not run by design in this pass:
    - targeted Vitest execution was skipped under the current local workflow preference because frontend tests on this machine are frequently unstable / blocking
- 2026-05-06: Completed the `CasesScreen` one-shot handoff and validate-error review follow-up.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx` so:
    - `initialProjectKey` / `initialCaseId` are consumed as one-shot handoff input only
    - manual project switching after a `cases` handoff is no longer overwritten by the stale handoff props
    - `handleValidateDsl()` now separates local JSON parse failure from backend/non-2xx validate failure
    - only local parse failure synthesizes `Invalid JSON`
    - backend validate failure now preserves the real error state and no longer fabricates `dslValidation`
  - Updated `ui/admin-console/src/App.tsx` so normal sidebar navigation into `cases` clears the stored case handoff project/id before remount.
  - Extended frontend coverage:
    - `ui/admin-console/src/App.test.tsx`
      - manual project switch still works after prepared-case handoff into `cases`
      - existing prepared-case handoff / clear-on-normal-navigation coverage kept passing
    - `ui/admin-console/src/screens/CasesScreen.test.tsx`
      - local invalid JSON stays local-only
      - validate non-2xx shows real error state instead of fake `Invalid JSON`
  - Verification:
    - `npm test -- --run src/App.test.tsx -t "allows manual project switching after a cases handoff without reapplying the old handoff|clears the one-shot prepared-case handoff when cases is reopened from normal navigation|opens cases from execution prepared-case drill-down via the existing App handoff"` in `ui/admin-console`: PASS
    - `npm run build` in `ui/admin-console`: PASS
  - Attempted but not used as gate:
    - `npm test -- --run src/screens/CasesScreen.test.tsx`
    - `npx vitest run src/screens/CasesScreen.test.tsx -t "shows invalid JSON feedback without sending validate request|keeps backend validation failures as real error state without synthesizing invalid JSON" --pool=forks --poolOptions.forks.singleFork`
    - this machine still shows the same Vitest hang / worker-startup instability for the `CasesScreen` test file
- 2026-05-06: Completed the `cases` History -> `reportDetail` handoff follow-up.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - added App-level `onOpenHistoryRun(runName)` callback wiring
    - `History` tab run rows are now clickable buttons instead of passive display rows
    - row click reuses the existing shell `reportDetail` handoff rather than adding any backend endpoint
  - Updated `ui/admin-console/src/App.tsx`:
    - passed existing `openReportDetail` into `CasesScreen` for history run-row drill-down
  - Extended frontend coverage:
    - `ui/admin-console/src/App.test.tsx`
      - added `cases` history -> `reportDetail` handoff regression coverage
    - `ui/admin-console/src/screens/CasesScreen.test.tsx`
      - added direct callback coverage for history run-row click
  - Synced Phase 3 docs and backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend endpoint
    - no new route system
    - first implementation still hands off by `runName` because case history payload has no dedicated canonical `runId`
  - Verification:
    - `npx vitest run src/App.test.tsx -t "opens cases from execution prepared-case drill-down via the existing App handoff"` in `ui/admin-console`: PASS
    - `npm run build` in `ui/admin-console`: PASS
  - Attempted but not used as gate:
    - `npx vitest run src/App.test.tsx -t "opens reportDetail from cases history run rows via the existing App handoff"`
    - `npx vitest run src/App.test.tsx -t "opens reportDetail from cases history run rows via the existing App handoff" --pool=forks --poolOptions.forks.singleFork`
    - `npx vitest run src/screens/CasesScreen.test.tsx -t "hands off history run rows through the existing app-level callback" --pool=forks --poolOptions.forks.singleFork`
    - this machine still shows the same Vitest hang / worker-startup instability for some targeted Vitest runs
- 2026-05-06: Completed the small `cases` history canonical-runId follow-up.
  - Updated `ui/admin-console/src/App.tsx`:
    - `openReportDetail()` no longer treats incoming history `runName` as canonical `runId`
    - added App-level `resolveReportRunId(runNameOrRunId)` helper
    - handoff now first matches `snapshot.reports` by `runId` or `runName`
    - resolved match stores canonical `runId`
    - unresolved match falls back to the original `runName`
  - Extended `ui/admin-console/src/App.test.tsx`:
    - `cases` history -> `reportDetail` now verifies snapshot-based `runName` -> `runId` resolution
    - added fallback coverage when no matching snapshot report exists
  - Synced docs and backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/reportDetail/interface-spec.md`
    - `docs/phase3/interface/reports/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend endpoint
    - no new route system
    - no reportDetail backend contract change
    - reuse existing App-level handoff only
  - Verification:
    - `npm run build` in `ui/admin-console`: PASS
  - Not run in this pass:
    - targeted Vitest cases for the new history handoff branch coverage
- 2026-05-07: Completed the small `reportDetail` canonical-runId doc cleanup follow-up.
  - Updated `docs/phase3/interface/reportDetail/functional-spec.md`:
    - removed leftover `selectedRunName` / `selected run name` wording
    - aligned selected-run wording to `selectedRunId` / `selectedReportRunId`
    - `Data diff` handoff rule now refers to the currently selected run id
  - Updated `docs/phase3/interface/reportDetail/interface-spec.md`:
    - `reports` -> `reportDetail` relationship now states canonical `runId`
    - identifier note no longer describes App-level selection as `runName`
  - Keep the explicit boundary:
    - no new interface
    - no route change
    - no backend contract change
    - no code change
    - no test run
- 2026-05-07: Completed the small `cases` sidebar recent-runs follow-up.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - sidebar `Recent runs` now reuses already loaded `historyState.data` instead of static placeholder bars
    - pass / fail / warn totals are derived from real run statuses for the current case
    - recent-run drill-down buttons reuse the existing `onOpenHistoryRun(runName)` App-level handoff
    - sidebar `Info` panel `Last run` now reflects the latest loaded history run when available
    - before history loads, sidebar text stays explicit about idle/loading/empty/error state rather than inventing synthetic run data
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no reportDetail backend contract change
    - no scheduler/report protocol change
    - no test run
- 2026-05-07: Completed the small `cases` sidebar plans follow-up.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - sidebar `Plans` now reuses already loaded `plansState.data` instead of static local plan rows
    - sidebar plan summary renders real `plans[]` data for the current case
    - sidebar preconditions render real `preconditions[]` data when available
    - before plans load, sidebar text stays explicit about idle/loading/empty/error state rather than inventing synthetic plan content
    - sidebar remains read-only and does not introduce any new handoff or editor path
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no report/cases backend contract change
    - no scheduler/report protocol change
    - no test run
- 2026-05-07: Completed the `cases` overview catalog editor restore follow-up.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - restored a visible catalog editor in the overview region
    - editor now reuses existing `onCaseChange`, `onAddCaseRow`, `onRemoveCaseRow`, and `onSubmit`
    - visible field set stays limited to `id`, `projectKey`, `name`, `tags`, `status`, and `archived`
    - save feedback is surfaced beside the editor through the existing `caseState`
    - detail canvas and backend-first DSL/state-machine/plans/history flows remain separate
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no execution/report/plugin protocol change
    - no test run
    - no build run
- 2026-05-07: Completed the tiny `cases` catalog-editor add-row context fix.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - overview editor `Add row` now forwards the current `selectedProjectKey`
  - Updated `ui/admin-console/src/App.tsx`:
    - new case draft rows now default to the forwarded project context when it is valid
    - fallback remains the existing first-project default when no valid cases project context is available
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no save-chain change
    - no detail-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` sidebar preload warmup follow-up.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - opening a case now preloads the existing `loadPlans()` and `loadHistory()` reads while keeping `activeTab` unchanged
    - sidebar `Plans` / `Info` / `Recent runs` now hydrate from the same warmed `plansState` / `historyState`
    - `Plans` / `History` tab activation now skips a redundant same-case reload when that state is already present
    - case-switch reset behavior still clears stale state before the next case preloads
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no App-level handoff semantic change
    - no DSL/state-machine save-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the tiny `cases` sidebar preload retry fix.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - same-case preload dedupe now skips only `loading` / `success`
    - failed same-case `plans` / `history` state no longer blocks manual tab retry
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no save-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` hero-summary placeholder cleanup.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - removed the fixed `14 runs this week | 100% pass` detail-hero placeholder
    - detail hero now reuses current-case `historyState` summary when available
    - when history is still preloading or unavailable, the hero now shows explicit state text instead of fake weekly stats
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no save-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` owner-placeholder cleanup.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - removed the fixed sidebar owner value `Lin Chen`
    - `Owner` now shows explicit not-provided text because current Phase 3 case data has no reusable owner field
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no save-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` assertion-count placeholder cleanup.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - replaced the fixed `5 assertions` step-card title value
    - assertion count now derives from the current `detailSteps` summary via `action === "assert"`
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no save-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` step-card derived-preview label cleanup.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - added explicit step-card note: `Local derived preview; not a backend-loaded execution timeline.`
    - kept the existing front-end-generated `detailSteps` model unchanged
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no save-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` step-row preview-field wording cleanup.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - selector cells now read as `Preview selector: ...`
    - value cells now read as `Sample value: ...` or explicit no-sample text
    - note/healed cells now read as `Derived note: ...` wording rather than runtime-authored execution detail
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no save-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` sample-literal placeholder cleanup.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - replaced the email-like account sample with `<preview-account>`
    - replaced the token-like sample with `<preview-token>`
    - replaced the DB-style assertion sample with `<preview-db-assertion>`
    - replaced the diff assertion sample with `<preview-diff-assertion>`
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no save-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` step-note preview wording cleanup.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - converted the most execution-like note sentences to preview/derived wording
    - examples now read as `Preview navigation step.`, `Preview submit step.`, `Preview URL assertion.`, `Preview DB assertion.`, and `Preview diff assertion.`
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no save-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` locator/path placeholder cleanup.
  - Updated `ui/admin-console/src/screens/CasesScreen.tsx`:
    - replaced the entry-path sample with `<preview-entry-path>`
    - replaced the healed-locator sample with `<preview-entry-locator>`
    - replaced the field-locator samples with `<preview-account-field>` and `<preview-token-field>`
    - replaced the submit-control and success-path samples with `<preview-submit-control>` and `<preview-success-path>`
  - Synced docs/backlog:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Keep the explicit boundary:
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no save-flow change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` derived-preview doc example alignment follow-up.
  - Updated `docs/phase3/interface/cases/interface-spec.md`:
    - replaced the two remaining `#primary-entry` DSL example targets with `<preview-entry-locator>`
    - aligned the example blocks with current derived-preview placeholder wording
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` derived-preview path-example alignment follow-up.
  - Updated `docs/phase3/interface/cases/interface-spec.md`:
    - replaced the remaining `"/checkout"` DSL example URLs with `<preview-entry-path>`
    - kept those example blocks aligned with current preview-placeholder wording
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` derived-preview residual-scan follow-up.
  - Scanned `docs/phase3/interface/cases/` for old sample shapes already removed from current `CasesScreen` preview UI:
    - `#primary-entry`
    - `/checkout`
    - `AUTO-E2E-2026`
    - `@demo.local`
    - `snapshot diff = expected`
    - `status = "done"`
    - `button.primary`
    - `[name=account]`
    - `[name=token]`
  - Result:
    - no remaining matches inside `docs/phase3/interface/cases/`
    - no additional doc正文 change was needed in this pass
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the canonical `runId` cross-screen terminology follow-up.
  - Updated `docs/phase3/interface/reports/interface-spec.md`:
    - current selected-run wording now uses canonical `runId`
    - remaining `runName -> runId` wording is limited to upstream history-adjacent fallback resolution
  - Updated `docs/phase3/interface/dataDiff/interface-spec.md`:
    - current run-context source now uses `selectedReportRunId`
    - App-level handoff now describes `openDataDiff(selectedReportRunId)`
    - relationship / identifier / remaining-limit notes no longer describe page entry as `selectedRunName`
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the canonical `runId` structured-model doc follow-up.
  - Updated `docs/phase3/interface/reports/interface-spec.md`:
    - `TimelineTarget` now uses `{ kind: "reportDetail"; runId: string }`
    - the paired `monitor` target remains `{ kind: "monitor"; runId?: string | null }`
  - Re-scanned adjacent `reportDetail` / `reports` / `dataDiff` specs:
    - no other structured-model conflicts remained in this target range
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the canonical `runId` monitor/dashboard terminology follow-up.
  - Updated `docs/phase3/interface/dashboard/interface-spec.md`:
    - downstream handoff now describes `dataDiff -> openDataDiff(runId ?? selectedReportRunId)`
  - Re-scanned:
    - `docs/phase3/interface/monitor/interface-spec.md`
    - `docs/phase3/interface/dashboard/functional-spec.md`
    - `docs/phase3/interface/dashboard/interface-spec.md`
    - no other direct `selectedRunName` / `selectedReportRunName` conflicts remained in this target range
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the canonical `runId` overview-doc terminology follow-up.
  - Re-scanned:
    - `docs/phase3/interface/ui-control-interface-overview.md`
    - `docs/phase3/interface/review-backlog.md`
  - Result:
    - no direct `selectedRunName` / `selectedReportRunName` / `selected run name` conflicts remained in this target range
    - no summary-doc正文 change was needed in this pass
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the canonical `runId` phase3-interface sweep follow-up.
  - Updated `docs/phase3/interface/reports/functional-spec.md`:
    - screen input wording now uses `selectedRunId`
    - report-detail handoff wording now describes `onOpenDetail(runId)`
    - selected-project follow rule now keys off `selectedRunId`
  - Updated `docs/phase3/interface/dataDiff/functional-spec.md`:
    - screen dependency wording now uses `selectedRunId`
    - selected-run context note now uses `selectReportViewModel(snapshot, selectedRunId)`
    - screen-input wording now says selected run id
  - Sweep result:
    - direct residual conflicts were limited to these two functional-spec files in this pass
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the canonical `runId` phase3-interface final sweep follow-up.
  - Re-scanned the full `docs/phase3/interface/` directory for direct canonical-runId terminology conflicts.
  - Result:
    - no new direct conflicts remained in this target range
    - no document正文 change was needed in this pass
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the `reportDetail` fallback wording cleanup follow-up.
  - Updated `docs/phase3/interface/reportDetail/functional-spec.md`:
    - summary-panel wording now says API report is primary and snapshot-derived view model is fallback
    - screenshot/assertion wording now distinguishes API-backed primary rendering from fallback view-model rendering
    - detail-model section now explicitly describes `GET /api/phase3/runs/{runId}/report` as primary and `selectReportViewModel(snapshot, selectedRunId)` as fallback-only
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the `reportDetail` interface-spec fallback review follow-up.
  - Re-reviewed `docs/phase3/interface/reportDetail/interface-spec.md` against current `ReportDetailScreen.tsx` semantics:
    - `GET /api/phase3/runs/{runId}/report` remains documented as the primary detail path
    - fallback to `selectReportViewModel(snapshot, selectedRunId)` is already documented
    - artifact, recovery, and AI-decisions sections already distinguish backend-backed reads from fallback/mock behavior
  - Result:
    - no interface-spec正文 change was needed in this pass
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases-history` runId fallback wording review follow-up.
  - Re-reviewed:
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/reports/interface-spec.md`
    - `docs/phase3/interface/reportDetail/interface-spec.md`
    - `docs/phase3/interface/dataDiff/interface-spec.md`
  - Result:
    - the current wording is already consistent for this remaining limit
    - `cases` explicitly states that the case-history payload has no dedicated canonical `runId`
    - `reports` / `dataDiff` already describe the corresponding history-adjacent `runName -> runId` resolution path
    - `reportDetail` does not introduce a contradictory statement
    - no正文 change was needed in this pass
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` sidebar data-source/failure-state wording review follow-up.
  - Re-reviewed:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Result:
    - the current wording is already consistent for this remaining limit
    - the docs already agree that the sidebar does not issue independent requests
    - the docs already agree that sidebar summaries reuse existing `plansState` / `historyState`
    - the docs already agree that failure/unloaded/empty states are surfaced explicitly instead of introducing a second data source
    - no正文 change was needed in this pass
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the `cases` plans/history read-only wording review follow-up.
  - Re-reviewed:
    - `docs/phase3/interface/cases/functional-spec.md`
    - `docs/phase3/interface/cases/interface-spec.md`
    - `docs/phase3/interface/review-backlog.md`
  - Result:
    - the current wording is already consistent for this remaining limit
    - the docs already agree that `Plans` / `History` are read-only on the current Phase 3 boundary
    - the docs already agree that no write interface or editor is exposed for these two areas today
    - the docs already agree that future write/edit capability can be added later
    - no正文 change was needed in this pass
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Completed the `reportDetail` fallback remaining-limit wording review follow-up.
  - Re-reviewed:
    - `docs/phase3/interface/review-backlog.md`
    - `docs/phase3/interface/reportDetail/functional-spec.md`
    - `docs/phase3/interface/reportDetail/interface-spec.md`
  - Result:
    - the current wording is already consistent for this remaining limit
    - the `reportDetail` docs already distinguish backend-first primary reads from snapshot-derived fallback on failure
    - `review-backlog.md` does not introduce a contradictory statement
    - no正文 change was needed in this pass
  - Keep the explicit boundary:
    - no code change
    - no new backend interface
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Planned the next Phase 3 backend manufacturing priorities in `review-backlog.md`.
  - Added new backlog tiers for the still-missing backend work:
    - `P3-1` canonical `docParse` document-list backend
    - `P3-2` canonical `runId` in `cases` history payload
    - `P3-3` backend-native report chain completion for `reports` / `reportDetail` / `dataDiff`
    - `P3-4` runtime/plugin realism upgrade for `monitor` / `plugin`
    - `P3-5` stronger backend services for `dataTemplates` / `models` / `environments`
    - `P4-1` typed summary/read models for `dashboard` / `projects` / `execution`
  - Updated suggested implementation order to follow those new priorities.
  - Keep the explicit boundary:
    - no code change
    - no new backend interface was implemented in this pass
    - no route change
    - no App-level handoff change
    - no backend-contract change
    - no test run
    - no build run
- 2026-05-07: Implemented `P3-1` canonical `docParse` document-list backend.
  - Backend:
    - added canonical `GET /api/phase3/documents`
    - `DocumentPersistenceService` now assembles persisted list metadata from existing `meta.json` + `parse-result.json`
    - optional `projectKey` query filtering is supported
  - Frontend:
    - `DocParseScreen` now prefers `GET /api/phase3/documents` for catalog rows
    - upload / re-parse / manual-save now refresh list metadata as well as detail reads
    - synthetic `buildDocuments(snapshot)` rows remain only as fallback shell entries
  - Docs:
    - synced `docParse` functional/interface specs
    - synced `ui-control-interface-overview.md`
    - marked `P3-1` done in `review-backlog.md`
  - Remaining limits:
    - fallback shell rows still exist for non-persisted preview entries
    - project-rail counts still partly reflect snapshot-derived fallback rows
    - no test run / no build run in this pass by explicit task boundary
- 2026-05-07: Implemented `P3-2` canonical `runId` in `cases` history payload.
  - Backend:
    - `GET /api/phase3/cases/{caseId}/history` default payload now includes dedicated `runId` for each run row
  - Frontend:
    - `CasesScreen` history-tab and sidebar recent-run clicks now prefer `run.runId ?? run.runName`
    - normal current payloads therefore hand off canonical `runId` directly into `reportDetail`
    - fallback to `runName` is kept only for older history payloads that still omit `runId`
  - Regression scaffolding:
    - case-history endpoint check now asserts `runId`
    - `CasesScreen` callback coverage now expects canonical `runId`
    - App-level history handoff coverage now uses direct backend `runId`, with a separate older-payload fallback case
  - Docs:
    - synced `cases` / `reports` / `reportDetail` / `dataDiff` adjacent specs
    - marked `P3-2` done in `review-backlog.md`
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Implemented the first small code slice of `P3-3` for a more backend-native `reports` list.
  - Backend:
    - `ReportArtifactService.buildSummary(...)` now includes `tags`
    - tag values are normalized from either list payloads or comma-delimited strings
    - fallback summaries now expose `tags: []`
  - Frontend:
    - `ReportsScreen` API-row mapping no longer re-matches `snapshot.cases` just to recover tags
    - canonical `GET /api/phase3/runs/` rows now provide the visible `caseTags` directly
    - `reportViewModel.ts` remains fallback-only when the run-list API is unavailable
  - Regression scaffolding:
    - report-artifact endpoint coverage now seeds `tags` into persisted `report.json`
    - list-endpoint assertion now checks that backend tags round-trip through `GET /api/phase3/runs/`
  - Docs:
    - synced `reports` functional/interface specs to reflect a backend-first list
    - updated `review-backlog.md` so `P3-3` now records the list-level summary-contract progress while leaving detail/diff fallback debt open
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Implemented the next small `P3-3` slice on the `dataDiff` chain by removing fake restore-result content.
  - Backend:
    - `GET /api/phase3/runs/{runId}/restore-result` no longer falls back to deterministic `PARTIAL` restore steps when `restore-result.json` is missing
    - the backend now returns an explicit empty shell instead:
      - `status: "UNAVAILABLE"`
      - `items: []`
  - Why `restore-result` first:
    - it was smaller and safer than `data-diff/raw`
    - `DataDiffScreen` already had an explicit empty-state branch for empty restore items, so no UI contract expansion was needed
  - Docs:
    - synced `dataDiff/interface-spec.md`
    - refined `review-backlog.md` so `restore-result` is no longer described as still using deterministic mock payloads
  - Remaining limits:
    - `data-diff/raw` still keeps deterministic mock fallback content
    - diff-table fallback rows and `reportDetail` snapshot fallback remain open
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Completed a tiny doc-only follow-up for the same `P3-3 dataDiff` restore-result cleanup.
  - Updated `docs/phase3/interface/dataDiff/interface-spec.md`:
    - the `GET /api/phase3/runs/{runId}/restore-result` example block no longer shows stale `PARTIAL` + fake steps
    - it now matches the current backend-missing-artifact semantics:
      - `runId`
      - `status: "UNAVAILABLE"`
      - `items: []`
  - Keep the explicit boundary:
    - no code change
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Implemented the next small `P3-3` slice on `dataDiff` by removing fake raw-diff content.
  - Backend:
    - `GET /api/phase3/runs/{runId}/data-diff/raw` no longer fabricates before/after/afterRestore entries from mock diff rows when `data-diff-raw.json` is missing
    - the backend now returns an explicit empty shell instead:
      - `status: "UNAVAILABLE"`
      - `before: []`
      - `after: []`
      - `afterRestore: []`
  - Frontend:
    - `DataDiffScreen` raw drawer still opens on the same endpoint
    - backend `UNAVAILABLE` shells now show explicit unavailable copy instead of empty fake JSON content
  - Docs:
    - synced `dataDiff/interface-spec.md`
    - refined `review-backlog.md` so `data-diff/raw` is no longer described as deterministic mock fallback
  - Remaining limits:
    - diff-table fallback rows still remain
    - `recovery` / `ai-decisions` still keep deterministic mock payloads
    - `reportDetail` snapshot fallback remains open
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Implemented the next small `P3-3` slice on `reportDetail` by removing fake AI decision logs.
  - Backend:
    - `GET /api/phase3/runs/{runId}/ai-decisions` no longer fabricates deterministic decision rows when `ai-decisions.json` is missing
    - the backend now returns an explicit empty shell instead:
      - `status: "UNAVAILABLE"`
      - `items: []`
  - Frontend:
    - `ReportDetailScreen` AI decisions panel now surfaces the backend `UNAVAILABLE` badge
    - existing `No AI decision data available` copy remains the empty-state body
  - Docs:
    - synced `reportDetail/interface-spec.md`
    - refined `review-backlog.md` so `ai-decisions` is no longer described as deterministic mock fallback
  - Remaining limits:
    - `recovery` deterministic mock fallback still remains
    - `reportDetail` snapshot fallback still remains on main detail read failures
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Implemented the next small `P3-3` slice on `reportDetail` by removing fake recovery steps.
  - Backend:
    - `GET /api/phase3/runs/{runId}/recovery` no longer fabricates deterministic recovery rows when `recovery.json` is missing
    - the backend now returns an explicit empty shell instead:
      - `status: "UNAVAILABLE"`
      - `items: []`
  - Frontend:
    - `ReportDetailScreen` recovery panel already had a status badge plus empty-state branch
    - the new shell is therefore consumed without extra panel refactor
  - Docs:
    - synced `reportDetail/interface-spec.md`
    - refined `review-backlog.md` so `recovery` is no longer described as deterministic mock fallback
  - Remaining limits:
    - `reportDetail` snapshot fallback still remains on main detail read failures
    - artifact/screenshot inline viewing is still not implemented
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Implemented the next small but more central `P3-3` slice on `reportDetail` by tightening the main report shell.
  - Backend:
    - missing `report.json` now surfaces as `status: "UNAVAILABLE"` in the main `GET /api/phase3/runs/{runId}/report` shell instead of generic `UNKNOWN`
  - Frontend:
    - `ReportDetailScreen` overview summary card now shows `No report available` for the backend-owned `UNAVAILABLE` shell
    - it no longer renders misleading 0/0 summary stats for the no-artifact case
  - Docs:
    - synced `reportDetail/interface-spec.md`
    - synced `reportDetail/functional-spec.md`
    - refined `review-backlog.md` so this pass is recorded as another concrete `P3-3` reduction of snapshot/fake dependence
  - Remaining limits:
    - snapshot-derived fallback still remains for broader backend read failures
    - artifact/screenshot inline viewing is still not implemented
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Implemented the next small but valuable `P3-3` slice on the main `dataDiff` payload by removing fake diff rows.
  - Backend:
    - `GET /api/phase3/runs/{runId}/data-diff` no longer fabricates deterministic diff rows when `data-diff.json` is missing or unreadable
    - the backend now returns an explicit empty shell instead:
      - `status: "UNAVAILABLE"`
      - zeroed `summary`
      - `rows: []`
  - Frontend:
    - `DataDiffScreen` main table panel now shows `No diff artifact available` for the backend-owned `UNAVAILABLE` shell
    - it no longer renders misleading synthetic stats/rows for the no-artifact case
    - synthetic local rows remain only as fallback when the main diff API read fails outright
  - Docs:
    - synced `dataDiff/interface-spec.md`
    - refined `review-backlog.md` so the main diff payload is no longer described as retaining the no-artifact synthetic-row path
  - Remaining limits:
    - diff-table fallback rows still remain for true diff-endpoint read failures
    - `reportDetail` snapshot fallback still remains on broader detail-read failures
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Implemented the next small but real `P3-3` artifact-read slice on `reportDetail` by enabling inline screenshot preview.
  - Backend:
    - added `GET /api/phase3/runs/{runId}/artifacts/content?path=...`
    - `ReportArtifactService` now validates run-local artifact paths and returns raw bytes plus inferred content type
  - Frontend:
    - `ReportDetailScreen` Overview screenshot cards now render `<img>` previews against the new content endpoint
    - screenshot previews prefer `steps[].artifactPath` when available, then fall back to screenshot artifacts
    - generic artifact drawer still remains listing-path-focused
  - Docs:
    - synced `reportDetail/interface-spec.md`
    - synced `reportDetail/functional-spec.md`
    - refined `review-backlog.md` so screenshot preview is no longer described as path-only
  - Remaining limits:
    - generic artifact drawer entries are still not expanded into broad inline/download handling
    - `reportDetail` snapshot fallback still remains on broader detail-read failures
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Applied a tiny follow-up fix on the new `reportDetail` screenshot content-read path semantics.
  - Frontend:
    - Overview screenshots still prefer `steps[].artifactPath`
    - when falling back to `apiReport.artifacts[]`, the content endpoint now uses the real `a.path`
    - it no longer misuses display `label` as the artifact-content query path
  - Tests:
    - added a focused App-level regression where `steps[].artifactPath` is absent and screenshot preview must come from `artifacts[].path`
  - Docs:
    - synced `reportDetail/interface-spec.md`
    - synced `reportDetail/functional-spec.md`
    - recorded the path-source priority in `01_dev_progress.md` / `memory.txt`
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Applied a small contract fix so artifact listing and artifact content-read now share one path semantic.
  - Backend:
    - `artifacts[].path` is now emitted as a run-local relative path instead of an absolute filesystem path
    - `GET /api/phase3/runs/{runId}/artifacts/content?path=...` keeps consuming that same run-local path contract
  - Frontend/tests:
    - Overview screenshot fallback already uses `a.path`; the fallback regression test now uses a realistic relative artifact path
    - backend test now asserts artifact list output also uses the relative path form
  - Docs:
    - synced `reportDetail/interface-spec.md`
    - synced `reportDetail/functional-spec.md`
    - recorded that `artifacts[].path` and `artifacts/content?path=...` now intentionally share one contract
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Pushed the same artifact-content endpoint one step further by making `report-html` previewable inside the artifact drawer.
  - Frontend:
    - the artifact drawer now exposes a `Preview` action for `report-html`
    - clicking it mounts an inline iframe backed by `GET /api/phase3/runs/{runId}/artifacts/content?path=...`
    - other artifact kinds still remain listing-path-focused for now
  - Why this slice:
    - `report-html` was the smallest stable text-like artifact to preview because the browser can render it directly from the existing content endpoint
    - this avoided expanding into a full multi-type artifact viewer
  - Docs:
    - synced `reportDetail/interface-spec.md`
    - synced `reportDetail/functional-spec.md`
    - refined `review-backlog.md` to record `report-html` as the first drawer-level inline preview type
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Extended the same tiny drawer-level reader path to `report-json`.
  - Frontend:
    - artifact drawer preview buttons now cover both `report-html` and `report-json`
    - `report-json` uses the existing content endpoint and renders inline as text in a `<pre>`
    - no generic multi-type viewer was introduced
  - Why this slice:
    - `report-json` was smaller and steadier than `log` because it is already present in current artifact fixtures
    - the endpoint already serves it as `application/json; charset=utf-8`, so no backend contract change was needed
  - Docs:
    - synced `reportDetail/interface-spec.md`
    - synced `reportDetail/functional-spec.md`
    - refined `review-backlog.md` so drawer-level inline preview now covers `report-html` and `report-json`
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Extended the same tiny drawer-level reader path one more step to `log`.
  - Backend:
    - `.log` artifacts are now classified as `kind: "log"` in artifact listings
  - Frontend:
    - reused the existing `report-json` text-preview path instead of creating a second viewer mechanism
    - `Preview runtime.log` now fetches the existing content endpoint and renders inline text in a `<pre>`
  - Why this slice:
    - it was the smallest follow-up after `report-json` because the drawer already had the text-preview state machine
    - no content-endpoint contract change was needed; `text/plain; charset=utf-8` already works
  - Docs:
    - synced `reportDetail/interface-spec.md`
    - synced `reportDetail/functional-spec.md`
    - refined `review-backlog.md` so drawer-level inline preview now covers `report-html`, `report-json`, and `log`
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Started `P3-4` on `monitor` by tightening `GET /api/phase3/runs/{runId}/live-page`.
  - Backend:
    - `RunStatusService` now accepts optional `runsRoot` context for runtime artifact reads
    - `live-page` now prefers `runs/<runId>/live-page.json` plus referenced or inferred run-local screenshot files
    - missing live-page artifacts no longer fall back to deterministic page shaping; the backend now returns:
      - `status: "UNAVAILABLE"`
      - empty title/url
      - `pageState: "unavailable"`
      - `screenshotPath: null`
    - available live-page artifacts now return:
      - `status: "AVAILABLE"`
      - run-local relative `screenshotPath`
  - Frontend:
    - `MonitorScreen` live-page panel now renders explicit unavailable copy for the backend-owned unavailable shell
    - when `screenshotPath` is present, the panel now renders inline `<img>` preview through the existing artifact-content endpoint
  - Tests/docs:
    - added backend regression for `AVAILABLE` live-page artifact vs `UNAVAILABLE` shell
    - added frontend regressions for unavailable live-page shell and inline screenshot preview
    - synced `monitor/interface-spec.md`, `monitor/functional-spec.md`, and `review-backlog.md`
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Continued `P3-4` on `monitor` by tightening `GET /api/phase3/runs/{runId}/runtime-log`.
  - Backend:
    - `RunStatusService` now prefers `runs/<runId>/runtime.log` when the run-local artifact exists
    - `runtime.log` lines are mapped into backend-owned runtime-log entries with:
      - `source: "runtime.log"`
      - `message` as raw line text
      - `detail.artifactPath = "runtime.log"`
      - `detail.line`
    - a small text-to-entry classifier now maps obvious content to `ERROR` / `WARNING` / `HEAL` / `DECISION` / `INFO`
    - when no `runtime.log` artifact exists, the endpoint keeps the existing scheduler-event-derived path but now marks it explicitly with:
      - `source: "scheduler-events"`
  - Tests/docs:
    - added backend regression for artifact-backed runtime-log priority plus scheduler-event fallback
    - synced `monitor/interface-spec.md`, `monitor/functional-spec.md`, and `review-backlog.md`
  - Why this slice:
    - it was the smallest real step on `P3-4` after `live-page` because it reuses the same run-local artifact-read boundary
    - the current `MonitorScreen` already understands `source` and runtime-log detail fields, so no broader front-end refactor was needed
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Continued `P3-4` on `monitor` by tightening `GET /api/phase3/runs/{runId}/steps`.
  - Backend:
    - `RunStatusService` now prefers run-local `report.json.steps[]` when that artifact already exists
    - monitor step payloads are now mapped from report steps via:
      - `label` from `stepName` / `action`
      - normalized `state`
      - `durationMs`
      - optional `startedAt`
      - optional `note` from `message` or `artifactPath`
    - when no run-local report-step artifact exists, the endpoint keeps the existing scheduler-event-derived and placeholder fallback path
  - Tests/docs:
    - added backend regression for report-backed step priority plus scheduler-event fallback
    - synced `monitor/interface-spec.md`, `monitor/functional-spec.md`, and `review-backlog.md`
  - Why this slice:
    - there was no obvious dedicated `steps.json`, but `report.json.steps[]` was already a real run-local artifact and was the smallest stronger source available
    - this reduces deterministic shaping without inventing a new runtime parser or contract
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Applied a small semantic follow-up on the same `P3-4 monitor /steps` path so report-backed failed steps no longer look pending.
  - Backend:
    - `normalizeReportStepState()` now preserves non-success terminal report states instead of collapsing them into `TODO`
    - `FAILED` / `ERROR`-like states map to `FAILED`
    - `SKIPPED` / `CANCELLED` / `ABORTED`-like states map to `SKIPPED`
  - Frontend:
    - `RunStep.state` now includes `FAILED` and `SKIPPED`
    - `MonitorScreen` step bar, step list, and detail badge now render distinct failed/skipped tones instead of todo styling
  - Tests/docs:
    - added backend assertion coverage for report-backed `FAILED` and `SKIPPED` step mapping
    - added `MonitorScreen` coverage so failed/skipped rows do not regress into todo semantics
    - synced `monitor/interface-spec.md`, `monitor/functional-spec.md`, and `review-backlog.md`
  - Why this slice:
    - it fixes the most misleading remaining report-backed `steps` semantic without expanding into the larger `status` chain
    - it keeps backend-owned truth on step terminal state instead of letting the front end fake pending work
  - Keep the explicit boundary:
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Applied a tiny doc-only alignment on `P3-4 monitor` for the live-page interface spec.
  - Docs:
    - `monitor/interface-spec.md` section `5.4 Live page viewport` no longer says `current state: placeholder only`
    - the section now matches the current real implementation and the later live-page contract details in the same file:
      - backend-owned `AVAILABLE` / `UNAVAILABLE` shell from `GET /api/phase3/runs/{runId}/live-page`
      - explicit unavailable copy in `MonitorScreen`
      - inline screenshot rendering through artifact-content when `screenshotPath` exists
  - Keep the explicit boundary:
    - no code change
    - no test run
    - no build run
    - no commit / push
- 2026-05-07: Continued `P3-4 monitor` by tightening `GET /api/phase3/runs/{runId}/status`.
  - Backend:
    - `RunStatusService` now prefers run-local `report.json` for stronger status/progress/assertion counts and run metadata
    - `currentPage.url` / `currentPage.state` now prefer run-local `live-page.json` when it exists
    - `lastUpdatedAt` now prefers the latest artifact-backed timestamp from `report.json`, `live-page.json`, or `runtime.log`, then falls back to scheduler events
    - when no stronger progress artifact exists, `/status` no longer fabricates the old default `8`-step shape and instead returns conservative `0 / 0 / 0` progress
    - scheduler persistence still remains the fallback shell for lifecycle status, `aiCalls`, `heals`, and control gating when stronger artifacts are absent
  - Tests/docs:
    - added backend coverage for artifact-backed `/status` priority plus conservative no-artifact fallback
    - synced `monitor/interface-spec.md`, `monitor/functional-spec.md`, and `review-backlog.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
  - Remaining limits:
    - `status` still falls back to a scheduler-derived shell when stronger run-local artifacts are absent
    - `steps` / `runtime-log` still keep their existing scheduler/placeholder fallback paths when those stronger artifacts are absent
- 2026-05-08: Continued `P3-4 plugin` by tightening `POST /api/phase3/extension/page-summary`.
  - Extension popup:
    - `popup.js` now sends `PAGE_SUMMARY_GET` with active-tab `pageTitle` / `pageUrl`, derived `pageDomain` / `pagePath`, cached popup runtime context (`runtimeMode`, `queueState`, `auditState`, `nextAction`), and optional locator
    - page identity fields now stay same-source: when `tab.url` exists, `pageUrl` / `pageDomain` / `pagePath` are all derived from that current tab URL; snapshot page fields only remain fallback
    - the popup caches the latest popup snapshot after refresh and reuses that runtime context when the operator requests page summary
    - the page-summary result panel now renders `domain`, `path`, and `runtimeSummary`
  - Backend:
    - `ExtensionActionService` now prefers caller-provided page/runtime fields over backend-local deterministic rules when they are present
    - page-summary responses now include `domain`, `path`, `runtimeSummary`, context-bound summary copy, and caller-preferred `recommendedAction`
  - Tests/docs:
    - popup test now asserts the richer `PAGE_SUMMARY_GET` payload plus rendered result fields
    - popup test also asserts that a stale `snapshot.page.domain` cannot override the current active-tab URL/domain/path
    - native-host test now asserts the richer page-summary payload is forwarded unchanged to local-admin-api
    - local-admin-api test now asserts page-summary response priority for domain/path/runtime summary/recommended action
    - synced `plugin/interface-spec.md`, `plugin/functional-spec.md`, and `review-backlog.md`
  - Verification:
    - ran `npm test -- --run popup.test.js`
    - ran `mvn -pl apps/native-host -Dtest=NativeHostMessageProcessorTest test`
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
  - Remaining limits:
    - `page-summary` still does not use DOM/content-script extraction for richer page understanding
    - quick smoke, pick mode, and platform handoff chains were intentionally left unchanged in this slice
- 2026-05-08: Continued `P3-4 plugin` by adding lightweight DOM context to `POST /api/phase3/extension/page-summary`.
  - Extension/content-script:
    - `content-script.js` now exposes `CS_PAGE_SUMMARY_CONTEXT_GET`
    - the content script returns a lightweight DOM snapshot with visible headings, form landmarks, primary action hints, and a short body summary
    - `background.js` now intercepts `PAGE_SUMMARY_GET`, asks the active tab content script for that DOM snapshot first, and merges it into the payload sent to native-host
    - when content-script messaging is unavailable, background safely falls back to the existing tab/runtime payload and still forwards the request
  - Backend:
    - `ExtensionActionService` now consumes caller-provided DOM context fields and folds them into page-summary signals and summary text
    - backend still remains caller-payload-first; no DOM parsing was moved into local-admin-api
  - Tests/docs:
    - background bridge test now asserts page-summary DOM-context enrichment and safe fallback
    - content-script test now asserts the lightweight DOM snapshot shape
    - local-admin-api test now asserts that page-summary summary text reflects caller-provided DOM context
    - synced `plugin/interface-spec.md`, `plugin/functional-spec.md`, and `review-backlog.md`
  - Verification:
    - ran `npm test -- --run background.test.js content-script.test.js popup.test.js`
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
  - Remaining limits:
    - `page-summary` still uses only a lightweight DOM snapshot; it does not yet perform richer structured page understanding
    - quick smoke, pick mode, and platform handoff chains were intentionally left unchanged in this slice
- 2026-05-08: Continued `P3-4 plugin` by deepening `PLATFORM_HANDOFF_PREPARE`.
  - Extension popup/background:
    - `popup.js` now reuses a shared current-page context builder for `PAGE_SUMMARY_GET` and `PLATFORM_HANDOFF_PREPARE`
    - `Open in platform` now sends current page title/url/domain/path, runtime context, and locator together with the existing execution handoff fields
    - `Use in DSL` now sends the same richer page/runtime identity fields together with the current real locator
    - `background.js` now applies the same lightweight content-script DOM snapshot enrichment to `PLATFORM_HANDOFF_PREPARE` that it already applies to `PAGE_SUMMARY_GET`
    - when content-script messaging is unavailable, platform handoff still safely falls back to the original caller payload
  - Backend/platform:
    - `ExtensionActionService.preparePlatformHandoff(...)` remains URL-assembly-only, but now includes richer caller-provided page/runtime/DOM context in the generated query params
    - `App.tsx` now consumes that richer query context into:
      - a more informative `execution` `launchForm.detail`
      - richer `aiGenerate` page-identity/runtime reasoning context
  - Tests/docs:
    - popup test now asserts richer `PLATFORM_HANDOFF_PREPARE` payloads for `Open in platform` and `Use in DSL`
    - background bridge test now asserts platform-handoff DOM-context enrichment
    - native-host test now asserts richer platform-handoff payload forwarding
    - local-admin-api test now asserts richer query-param handoff URLs
    - App test now asserts plugin execution / AI-generate query handoff consumption still works with those richer params
    - synced `plugin/interface-spec.md`, `plugin/functional-spec.md`, and `review-backlog.md`
  - Verification:
    - ran `npm test -- --run popup.test.js background.test.js`
    - ran `npx vitest run src/App.test.tsx -t "consumes plugin"`
    - ran `mvn -pl apps/native-host -Dtest=NativeHostMessageProcessorTest test`
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
  - Remaining limits:
    - platform handoff still uses lightweight query params plus App-local state mapping; it does not introduce typed route models or persisted cross-app context
    - `Use in DSL` still targets `aiGenerate`, not direct `cases` DSL insertion
- 2026-05-08: Continued `P3-4 plugin` by tightening `Quick smoke test`.
  - Extension popup/background:
    - `popup.js` now reuses the shared current-page context builder for `buildSchedulerRequestPayload(...)`
    - quick-smoke scheduler requests now carry page title/url/domain/path, runtime context, locator, and the existing scheduler request fields together
    - `background.js` now applies the same lightweight content-script DOM snapshot enrichment to `SCHEDULER_REQUEST_CREATE` that it already applies to `PAGE_SUMMARY_GET` and `PLATFORM_HANDOFF_PREPARE`
    - when content-script messaging is unavailable, quick-smoke request forwarding safely falls back to the original scheduler payload
  - Backend:
    - `SchedulerPersistenceService.appendRequest(...)` now persists richer page/runtime/DOM startup context fields on scheduler request entries when present
    - the canonical scheduler request shape remains compatible and the richer fields stay additive
  - Tests/docs:
    - popup test now asserts richer `SCHEDULER_REQUEST_CREATE` payload shaping
    - background bridge tests now assert quick-smoke DOM-context enrichment and safe fallback
    - native-host test now asserts richer scheduler request payload forwarding
    - local-admin-api test now asserts scheduler request persistence remains compatible while retaining richer context fields
    - synced `plugin/interface-spec.md`, `plugin/functional-spec.md`, and `review-backlog.md`
  - Verification:
    - ran `npm test -- --run popup.test.js background.test.js`
    - ran `mvn -pl apps/native-host -Dtest=NativeHostMessageProcessorTest test`
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
  - Remaining limits:
    - richer quick-smoke startup context is now persisted on scheduler request entries, but downstream execution/monitor/report consumers still do not actively surface most of those fields yet
    - quick smoke still remains a lightweight scheduler enqueue action rather than a fuller popup execution runtime
- 2026-05-08: Continued `P3-4 monitor` by letting downstream fallback consume persisted scheduler request context.
  - Backend:
    - `RunStatusService.getRunStatus(...)` now prefers persisted request `pageUrl` for scheduler-only `currentPage.url` fallback instead of only `targetUrl`
    - scheduler-only `currentPage.state` now prefers a compact runtime summary derived from persisted `runtimeMode`, `queueState`, and `auditState`
    - `GET /api/phase3/runs/{runId}/live-page` unavailable shells now carry persisted request-backed `url`, `title`, runtime-summary `pageState`, `nextAction`/`bodySummary` context, and `locator` target when those fields exist
    - live-page available shells also now reuse request-backed `pageUrl`, `pageTitle`, and `locator` fallback more consistently when live artifacts are partial
  - Frontend:
    - `MonitorScreen` now keeps the explicit unavailable live-page copy but also renders the returned shell context (`title`, `url`, `pageState`, context, locator`) instead of hiding it
  - Tests/docs:
    - backend monitor test now asserts richer request context survives into `/status` fallback and `/live-page` unavailable shells
    - frontend monitor test now asserts unavailable live-page rendering shows the richer fallback context
    - synced `monitor/interface-spec.md`, `monitor/functional-spec.md`, and `review-backlog.md`
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
  - Remaining limits:
    - downstream execution/report chains still do not broadly consume all persisted quick-smoke scheduler context fields
    - `monitor/status` still remains a scheduler-derived shell when strong run-local artifacts are absent; this slice only made that shell more truthful
- 2026-05-12: Continued `P3-4 monitor` by tightening `GET /api/phase3/runs/{runId}/runtime-log` fallback one more step.
  - Backend:
    - `RunStatusService.getRunRuntimeLog(...)` keeps the explicit priority:
      - `runtime.log` artifact
      - non-step scheduler events
      - persisted scheduler request context shell
    - when no run-local `runtime.log` exists and scheduler events do not yield usable runtime-log rows, the backend now emits a few `source: "scheduler-request-context"` entries derived from persisted request fields:
      - page identity from `pageTitle` / `pageUrl`
      - runtime summary from `runtimeMode` / `queueState` / `auditState`
      - operator guidance from `nextAction` / `bodySummary`
      - locator cue from `locator`
  - Tests/docs:
    - backend monitor test now asserts request-context runtime-log fallback in addition to artifact-first and scheduler-event fallback precedence
    - synced `monitor/interface-spec.md`, `monitor/functional-spec.md`, and `review-backlog.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
  - Remaining limits:
    - runtime-log request-context fallback is still a compact shell, not a richer structured runtime artifact model
    - if persisted scheduler requests also lack page/runtime/locator context, fallback runtime-log can still remain sparse
- 2026-05-12: Applied a small review follow-up on the same `P3-4 monitor /runtime-log` request-context slice.
  - Backend:
    - `buildRequestContextRuntimeLogEntries(...)` no longer lets `runId` by itself trigger the `Prepared page context...` shell entry
    - that page-context entry now appears only when real page identity fields such as `pageTitle` or `pageUrl` exist
  - Tests:
    - backend monitor test now includes a `runId`-only scheduler request case and asserts the runtime-log fallback stays empty instead of fabricating a misleading page-context entry
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
- 2026-05-12: Continued `P3-4 monitor` by tightening `GET /api/phase3/runs/{runId}/steps` fallback.
  - Backend:
    - `RunStatusService.getRunSteps(...)` keeps the effective priority:
      - `report.json.steps[]`
      - scheduler `STEP_*` events
      - empty fallback
    - removed the old hardcoded placeholder-step generator
    - when no run-local report steps exist and scheduler does not provide real `STEP_*` events, the backend now returns `items: []` instead of fabricating an 8-step placeholder flow
  - Tests/docs:
    - backend monitor test now asserts the empty fallback when only non-step scheduler events exist
    - synced `monitor/interface-spec.md`, `monitor/functional-spec.md`, and `review-backlog.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
  - Remaining limits:
    - `steps` empty fallback is more honest than the old placeholder flow, but there is still no dedicated unavailable-state contract for the step timeline
- 2026-05-12: Continued the same `P3-4 monitor` line with a front-end empty-step copy follow-up.
  - Frontend:
    - `MonitorScreen` now shows explicit no-step copy in the progress area when the step bar is empty
    - the step-list panel now states that neither report step artifacts nor scheduler step timeline data are available yet
  - Tests/docs:
    - monitor front-end test now asserts the new empty-step copy
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-12: Continued the same `P3-4 monitor` line by adding a backend-owned `/steps` availability marker.
  - Backend:
    - `GET /api/phase3/runs/{runId}/steps` now includes `availability`
    - `availability: "AVAILABLE"` when report-backed or scheduler-backed step rows exist
    - `availability: "UNAVAILABLE"` when the endpoint returns the empty fallback
  - Frontend:
    - `RunStepsResponse` now accepts the additive `availability` field
    - `MonitorScreen` now prefers that backend marker when deciding whether to show the no-step copy, while still falling back to the old empty-list interpretation for compatibility
  - Tests/docs:
    - backend monitor test now asserts step availability across artifact-backed, scheduler-backed, and empty-fallback cases
    - front-end monitor test now feeds `availability: "UNAVAILABLE"` into the empty-step case
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-12: Applied a small review follow-up on the same `P3-4 monitor /steps availability` slice.
  - Frontend:
    - both no-step render sites in `MonitorScreen` now use the same derived `showUnavailableSteps` boolean instead of split marker-vs-length checks
    - the compatibility fallback remains centralized in `resolveStepsAvailability(...)`
  - Tests:
    - `MonitorScreen.test.tsx` now includes a legacy `/steps` payload without `availability` and asserts the two no-step copy blocks still render
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-12: Continued the same additive-marker pattern on `P3-4 monitor /runtime-log`.
  - Backend:
    - `GET /api/phase3/runs/{runId}/runtime-log` now includes `availability`
    - `availability: "AVAILABLE"` when artifact rows, scheduler-event rows, or request-context shell rows exist
    - `availability: "UNAVAILABLE"` only when all three runtime-log sources are empty and the endpoint returns `items: []`
  - Frontend:
    - `RuntimeLogResponse` now accepts the additive `availability` field
    - `MonitorScreen` now prefers that backend marker for runtime-log empty-state rendering and still falls back to the old empty-list interpretation when legacy payloads omit the marker
  - Tests/docs:
    - backend monitor test now asserts runtime-log availability across artifact-backed, scheduler-event-backed, request-context-backed, and empty-fallback cases
    - front-end monitor test now covers both marker-driven and legacy runtime-log empty payloads
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-12: Continued the same additive-marker pattern on `P3-4 monitor /runtime-log` with a top-level provenance hint.
  - Backend:
    - `GET /api/phase3/runs/{runId}/runtime-log` now also includes `sourceLayer`
    - `sourceLayer` values:
      - `RUNTIME_ARTIFACT`
      - `SCHEDULER_EVENTS`
      - `REQUEST_CONTEXT`
      - `NONE`
  - Frontend:
    - `RuntimeLogResponse` now accepts the additive `sourceLayer` field
    - `MonitorScreen` now shows a lightweight source hint in the runtime-log panel header
    - when the marker is absent, the screen falls back to minimal inference from existing entry `source` values or empty-list semantics for legacy compatibility
  - Tests/docs:
    - backend monitor test now asserts runtime-log source-layer values across artifact-backed, scheduler-event-backed, request-context-backed, and empty-fallback cases
    - front-end monitor test now covers marker-driven source hints plus a legacy empty runtime-log payload without `sourceLayer`
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-13: Continued the same provenance-marker pattern on `P3-4 monitor /steps`.
  - Backend:
    - `GET /api/phase3/runs/{runId}/steps` now includes `sourceLayer`
    - `sourceLayer` values:
      - `REPORT_ARTIFACT`
      - `SCHEDULER_EVENTS`
      - `NONE`
  - Frontend:
    - `RunStepsResponse` now accepts the additive `sourceLayer` field
    - `MonitorScreen` now shows a lightweight steps source hint in the panel header
    - when the marker is absent, the screen falls back to minimal legacy inference from `items` plus the existing availability/fallback rules
  - Tests/docs:
    - backend monitor test now asserts step source-layer values across report-backed, scheduler-backed, and empty-fallback cases
    - front-end monitor test now covers marker-driven step source hints plus a legacy empty-step payload without `sourceLayer`
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-13: Continued the same provenance-marker pattern on `P3-4 monitor /live-page`.
  - Backend:
    - `GET /api/phase3/runs/{runId}/live-page` now includes `sourceLayer`
    - `sourceLayer` values:
      - `LIVE_ARTIFACT`
      - `REQUEST_CONTEXT`
      - `NONE`
    - `LIVE_ARTIFACT` is emitted when run-local `live-page.json` or screenshot artifacts back the response
    - `REQUEST_CONTEXT` is emitted when no live artifact exists but the unavailable shell still carries persisted scheduler request context
    - `NONE` is emitted when no live artifact and no request-context shell are available
  - Frontend:
    - `LivePage` now accepts the additive `sourceLayer` field
    - `MonitorScreen` now shows a lightweight live-page source hint in the panel header
    - when the marker is absent, the screen falls back to minimal legacy inference from `status`, `screenshotPath`, and existing shell fields
  - Tests/docs:
    - backend monitor test now asserts live-page source-layer values across artifact-backed, request-context-backed, and no-source responses
    - front-end monitor test now covers marker-driven live-page hints plus a legacy unavailable live-page payload without `sourceLayer`
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-13: Continued the same provenance-marker pattern on `P3-4 monitor /status`.
  - Backend:
    - `GET /api/phase3/runs/{runId}/status` now includes `sourceLayer`
    - `sourceLayer` values:
      - `RUN_ARTIFACTS`
      - `SCHEDULER_FALLBACK`
    - `RUN_ARTIFACTS` is emitted when run-local `report.json`, `live-page.json`, or runtime artifact timestamp context strengthens the status response
    - `SCHEDULER_FALLBACK` is emitted when the response still resolves to the scheduler-backed shell
  - Frontend:
    - `RunStatus` now accepts the additive `sourceLayer` field
    - `MonitorScreen` now shows a lightweight status source hint in the hero row
    - when the marker is absent, the screen falls back only to stronger artifact-like current-page signals such as `artifact-captured` and otherwise stays conservative with `SCHEDULER_FALLBACK`
  - Tests/docs:
    - backend monitor test now asserts status source-layer values across artifact-strengthened and scheduler-fallback responses
    - front-end monitor test now covers marker-driven status hints plus a legacy status payload without `sourceLayer`
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-13: Tightened `P3-4 monitor /status` counters without changing the response shape.
  - Backend:
    - `status.counters.aiCalls` / `status.counters.heals` now prefer stronger artifact-backed sources before scheduler-event fallback
    - priority is:
      - report summary counters when present
      - run-local `runtime.log` line classification
      - scheduler-event counts
    - this keeps `/status` structure unchanged while reducing stale scheduler-only counts for artifact-backed runs
  - Tests/docs:
    - backend monitor test now asserts artifact-backed `aiCalls` / `heals` win over conflicting scheduler-event counts
    - the fallback status path still asserts scheduler-event `aiCalls` / `heals` when no artifact-backed counter source exists
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
- 2026-05-13: Added a lightweight `summary` field to `P3-4 monitor /live-page`.
  - Backend:
    - `GET /api/phase3/runs/{runId}/live-page` now optionally includes top-level `summary`
    - priority is:
      - `live-page.json.summary`
      - persisted scheduler request `bodySummary`
      - persisted scheduler request `nextAction`
    - when neither artifact nor request-context summary exists, the field is omitted
  - Frontend:
    - `LivePage` now accepts optional `summary`
    - `MonitorScreen` now renders that summary text in the `Live page` panel for both artifact-backed and request-context-backed shells
    - screenshot / highlight / locator rendering stays unchanged
  - Tests/docs:
    - backend monitor test now asserts artifact-backed and request-context-backed live-page summaries, plus omission on the no-source branch
    - front-end monitor test now asserts live-page summary rendering for artifact-backed, request-context-backed, and legacy live-page payloads
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-13: Started the first small persisted control-state readback slice for `P3-4 monitor`.
  - Backend:
    - `POST /api/phase3/runs/{runId}/pause` now records a persisted `PAUSING` phase event instead of jumping straight to `PAUSED`
    - `POST /api/phase3/runs/{runId}/abort` now records a persisted `ABORTING` phase event instead of jumping straight to `ABORTED`
    - `GET /api/phase3/runs/{runId}/status` now reads those persisted phase events back before any stronger artifact-backed terminal status overrides them
    - `control.canPause` / `control.canAbort` now align more closely with these phase states:
      - `PAUSING` disables pause
      - `ABORTING` disables both pause and abort
  - Frontend:
    - `MonitorScreen` consumes the existing `status` / `control` fields only; no new endpoint or extra control contract was added
    - added a front-end regression asserting `ABORTING` disables both buttons
  - Tests/docs:
    - backend monitor test now asserts pause writes `PAUSING`, abort writes `ABORTING`, and `/status` reads both phase states back with aligned control booleans
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-13: Added lightweight control-request readback fields to `monitor/status`.
  - Backend:
    - when `status` is `PAUSING` or `ABORTING`, `status.control` now reuses the latest matching persisted control event to expose:
      - `requestedBy`
      - `requestReason`
      - `requestedAt`
    - the fields are sourced from the existing persisted event `owner` / `detail` / `at`
  - Frontend:
    - `RunStatus.control` now accepts those additive fields
    - `MonitorScreen` now shows a lightweight control summary line in the hero actions area so operators can see who requested the phase change, why, and when
  - Tests/docs:
    - backend monitor test now asserts pause/abort `/status.control` readback fields
    - front-end monitor test now asserts the aborting-state summary line renders
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-13: Extended the same persisted control readback line into `monitor/runtime-log`.
  - Backend:
    - when `runtime.log` artifact does not override control semantics, persisted `PAUSING` / `ABORTING` scheduler events now emit backend-owned scheduler-event log rows
    - those rows keep the existing runtime-log entry shape and reuse:
      - `source: "scheduler-events"`
      - control-specific `summary`
      - `message` for the request reason
      - `detail.requestedBy` / `detail.requestedAt` / `detail.requestReason`
  - Tests/docs:
    - backend monitor test now asserts pause/abort `/runtime-log` exposes the corresponding control rows
    - no front-end code change was needed because the existing runtime-log row/detail UI can already render `message` / `detail`
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
- 2026-05-13: Extended the same control-intent visibility line into request-context-backed `monitor/live-page` shells.
  - Backend:
    - when `live-page` falls back to a request-context-backed `UNAVAILABLE` shell and `status` is `PAUSING` / `ABORTING`, the lightweight `summary` now appends a control note
    - the note explains that pause/abort was requested, who requested it, and why
    - artifact-backed live-page payloads and no-source shells are left unchanged
  - Tests/docs:
    - backend monitor test now asserts both `PAUSING` and `ABORTING` request-context-backed live-page shells include the appended control note
    - the no-source branch still asserts no summary is emitted
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
- 2026-05-13: Added a pure-UI control-phase banner to `MonitorScreen` for persisted `PAUSING` / `ABORTING` states.
  - Frontend:
    - when `runStatus.status` is `PAUSING` or `ABORTING`, `MonitorScreen` now shows a dedicated control-phase banner
    - the banner explicitly says the control request is still in progress
    - it warns operators that runtime log, step timeline, and live-page panels may temporarily remain on an older snapshot while the control phase settles
    - it reuses the existing `requestedBy`, `requestReason`, and `requestedAt` readback fields when present
  - Tests/docs:
    - front-end monitor regression now covers both `PAUSING` and `ABORTING`
    - the assertions keep banner copy and button-disabled behavior coupled in the same scenarios
    - synced `monitor/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-13: Made monitor empty-state copy more control-phase-aware without changing any API contract.
  - Frontend:
    - when `steps` are unavailable during `PAUSING` / `ABORTING`, `MonitorScreen` now explains that newer scheduler/report step snapshots may not have arrived yet
    - when `runtimeLog` is empty during `PAUSING` / `ABORTING`, the empty-state copy now explains that newer log entries may still be pending behind the control phase
    - when `livePage` is `UNAVAILABLE` during `PAUSING` / `ABORTING`, the panel now also adds a control-phase-specific empty-state note ahead of the existing request-context shell fields
  - Tests/docs:
    - front-end monitor regression now covers `PAUSING` empty steps plus `ABORTING` empty runtime-log / unavailable live-page cases
    - synced `monitor/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Added immediate local control-request feedback for monitor `pause` / `abort`.
  - Backend:
    - accepted `POST /api/phase3/runs/{runId}/pause` and `abort` responses now also echo:
      - `requestedBy`
      - `requestReason`
      - `requestedAt`
    - those values are aligned with the persisted control event by explicitly writing `requestedAt` into the event payload before append
  - Frontend:
    - `App.tsx` now returns the accepted control response immediately instead of waiting for snapshot reload to finish
    - `MonitorScreen` now keeps a small optimistic control-response overlay
    - after a successful pause/abort click, the screen can update the control-phase banner / request summary / button gating immediately, even before `/status` has read back the new phase
    - that optimistic overlay is cleared once `/status` reaches either the same requested phase or a direct stronger terminal handoff such as `PAUSING -> PAUSED` or `ABORTING -> ABORTED`
  - Tests/docs:
    - backend monitor test now asserts accepted pause/abort POST responses include `requestedBy` / `requestReason` / `requestedAt`
    - front-end monitor test now covers immediate pause/abort feedback driven by the POST response rather than only by later status polling
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Extended monitor control feedback to non-accepted pause/abort results.
  - Backend/tests:
    - `LocalAdminApiServerTest` now explicitly covers the remaining reject/idempotent readback cases:
      - `ALREADY_PAUSED`
      - `ALREADY_ABORTED`
      - `REJECTED`
  - Frontend:
    - `App.tsx` no longer throws immediately on non-`ACCEPTED` monitor control responses
    - `MonitorScreen` now shows inline warning feedback for `ALREADY_PAUSED`, `ALREADY_ABORTED`, and `REJECTED`
    - that warning is rendered immediately before the refresh resolves, and the screen then forces a fresh monitor snapshot/status reload so the operator sees the latest persisted state instead of only an action error
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Deduplicated monitor control refresh ownership on the front end.
  - Frontend:
    - `App.tsx` monitor `onPauseRun` / `onAbortRun` handlers no longer call top-level `loadSnapshot()`
    - `MonitorScreen` remains the single owner of post-control monitor refresh via `fetchMonitorData(runId)`
    - accepted optimistic feedback and non-accepted warning+refresh behavior are unchanged
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "keeps monitor control refresh ownership inside MonitorScreen"`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Started shrinking monitor footer snapshot dependence by moving queue-pressure text to run-local status first.
  - Backend:
    - `GET /api/phase3/runs/{runId}/status` now adds optional top-level `queueState` from persisted request context when available
  - Frontend:
    - `RunStatus` now accepts optional `queueState`
    - monitor footer `Queue pressure` now prefers `runStatus.queueState`
    - when `queueState` is absent, the footer still falls back to `snapshot.workQueue[0].detail`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Continued shrinking monitor footer snapshot dependence by moving last-event text to run-local status first.
  - Backend:
    - `GET /api/phase3/runs/{runId}/status` now adds optional top-level `lastEventSummary`
    - the current summary is derived from the latest scheduler-backed event detail/title/type/status in that order
  - Frontend:
    - `RunStatus` now accepts optional `lastEventSummary`
    - monitor footer `Last event` now prefers `runStatus.lastEventSummary`
    - when `lastEventSummary` is absent, the footer still falls back to `snapshot.timeline[0]` copy
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Tightened monitor `lastEventSummary` / `lastEventAt` priority inside `/status`.
  - Backend:
    - when `/status.sourceLayer` is effectively artifact-strengthened, `RunStatusService` now prefers stronger artifact-derived last-event context before falling back to the latest scheduler event
    - current artifact priority is:
      - report artifact updated/status context
      - live-page artifact updated context
      - runtime.log artifact modified-time context
      - latest scheduler event fallback
  - Tests/docs:
    - `LocalAdminApiServerTest` now asserts artifact-backed status returns `lastEventSummary` / `lastEventAt` from the artifact path rather than the older scheduler detail
    - synced `monitor/interface-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
- 2026-05-14: Added a tiny provenance marker for monitor footer `Last event`.
  - Backend:
    - `GET /api/phase3/runs/{runId}/status` now also includes additive `lastEventSource`
    - values are kept minimal:
      - `ARTIFACT`
      - `SCHEDULER`
      - `NONE`
    - the marker follows the current last-event priority:
      - artifact-backed `lastEventSummary` / `lastEventAt` -> `ARTIFACT`
      - latest scheduler-event fallback -> `SCHEDULER`
      - no run-local event context -> `NONE`
    - the missing-run / no-event path was also tightened so `lastEventAt` no longer dereferences a null latest event while producing the `NONE` marker
  - Frontend:
    - `RunStatus` now accepts optional `lastEventSource`
    - monitor footer `Last event` now shows a lightweight source hint beside the summary/time text
    - when older status payloads omit the marker, `MonitorScreen` keeps a minimal compatibility inference path rather than breaking the footer
  - Tests/docs:
    - `LocalAdminApiServerTest` now asserts artifact / scheduler / none `lastEventSource` branches
    - `MonitorScreen.test.tsx` now covers source hints plus legacy missing-marker compatibility
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Added the same lightweight provenance treatment to monitor footer `Queue pressure`.
  - Backend:
    - `GET /api/phase3/runs/{runId}/status` now also includes additive `queueStateSource`
    - the backend values stay minimal:
      - `REQUEST_CONTEXT`
      - `NONE`
    - when persisted scheduler request context provides `queueState`, `/status` marks it as `REQUEST_CONTEXT`
    - when run-local status does not provide queue context, the backend stays conservative with `NONE` and does not invent snapshot-owned semantics
  - Frontend:
    - `RunStatus` now accepts optional `queueStateSource`
    - monitor footer `Queue pressure` now shows a lightweight source hint beside the queue text
    - when older status payloads omit the marker, `MonitorScreen` keeps compatibility by inferring:
      - `REQUEST_CONTEXT` from run-local `queueState`
      - `SNAPSHOT_FALLBACK` from the existing parent snapshot footer fallback
      - `NONE` only when neither source exists
  - Tests/docs:
    - `LocalAdminApiServerTest` now asserts request-context and none `queueStateSource` branches
    - `MonitorScreen.test.tsx` now covers explicit source hints plus legacy missing-marker compatibility
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Refactored monitor footer snapshot fallbacks into explicit helper resolvers without changing behavior.
  - Frontend:
    - `MonitorScreen` no longer reads `snapshot.workQueue[0]` and `snapshot.timeline[0]` inline in JSX
    - queue footer now goes through a dedicated resolver that makes the order explicit:
      - run-local `queueState` first
      - snapshot queue detail second
      - none last
    - last-event footer now goes through a dedicated resolver that makes the order explicit:
      - run-local `lastEventSummary` / `lastEventAt` first
      - snapshot timeline fallback second
      - none last
  - Tests:
    - `MonitorScreen.test.tsx` keeps the existing queue/last-event precedence coverage green
    - added an extra footer regression for the explicit `none last` helper path when both run-local and snapshot context are absent
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Started the first real monitor footer fallback removal on `Queue pressure`.
  - Frontend:
    - when selected `runId` status payload now explicitly carries `queueStateSource: "NONE"`, `MonitorScreen` no longer falls back to parent `snapshot.workQueue[0].detail`
    - that branch now shows an explicit run-local none copy instead:
      - `No run-local queue context is available yet.`
    - compatibility boundary is preserved:
      - explicit marker `NONE` -> trust run-local none semantics
      - legacy payload missing `queueStateSource` -> keep the older snapshot fallback
  - Tests/docs:
    - `MonitorScreen.test.tsx` now asserts explicit `queueStateSource: "NONE"` suppresses the snapshot queue fallback
    - existing legacy fallback coverage remains intact
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Applied the same first real footer fallback removal pattern to `Last event`.
  - Frontend:
    - when selected `runId` status payload now explicitly carries `lastEventSource: "NONE"`, `MonitorScreen` no longer falls back to parent `snapshot.timeline[0]`
    - that branch now shows explicit run-local none copy instead:
      - `No run-local event context is available yet.`
    - compatibility boundary remains aligned with `Queue pressure`:
      - explicit marker `NONE` -> trust run-local none semantics
      - legacy payload missing `lastEventSource` -> keep the older snapshot timeline fallback
  - Tests/docs:
    - `MonitorScreen.test.tsx` now asserts explicit `lastEventSource: "NONE"` suppresses the snapshot timeline fallback
    - existing legacy fallback coverage remains intact
    - synced `monitor/interface-spec.md` and `monitor/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Explicitly consolidated selected-run snapshot compatibility into `legacyMonitorFallback`.
  - Frontend:
    - `MonitorScreen` now reads the remaining selected-run snapshot compatibility inputs through a single helper:
      - queue fallback detail
      - last-event fallback detail/title
    - queue footer and last-event footer now consume that helper instead of each reading `snapshot.workQueue[0]` / `snapshot.timeline[0]` directly
    - behavior stays unchanged:
      - legacy payloads still keep the existing snapshot fallback
      - explicit `queueStateSource: "NONE"` / `lastEventSource: "NONE"` still win and are not overridden by the compatibility layer
  - Tests:
    - existing queue/last-event fallback regressions remain green
    - added a combined regression proving the new helper does not override explicit none markers
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Tightened `Queue pressure` again so modern markers bypass `legacyMonitorFallback` completely.
  - Frontend:
    - `resolveQueuePressureFooter()` now treats the presence of a modern `queueStateSource` marker as a hard boundary
    - once that marker exists:
      - `REQUEST_CONTEXT` -> only run-local `queueState` is allowed to render
      - `NONE` -> only run-local none copy is allowed to render
    - `legacyMonitorFallback` is now only consulted for queue footer when the marker is absent in older payloads
  - Tests:
    - added a regression proving even malformed modern payloads (`queueStateSource: "REQUEST_CONTEXT"` with missing `queueState`) still do not backfill from snapshot
    - existing legacy fallback and explicit-none regressions remain green
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Applied the same modern-marker hard boundary to `Last event`.
  - Frontend:
    - `resolveLastEventFooter()` now treats the presence of a modern `lastEventSource` marker as a hard boundary
    - once that marker exists:
      - `ARTIFACT` / `SCHEDULER` -> only run-local `lastEventSummary / lastEventAt` may render
      - `NONE` -> only run-local none copy may render
    - `legacyMonitorFallback` is now only consulted for last-event footer when the marker is absent in older payloads
  - Tests:
    - added a regression proving malformed modern payloads (`lastEventSource: "SCHEDULER"` with missing summary/time) still do not backfill from snapshot timeline
    - existing legacy fallback and explicit-none regressions remain green
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Split selected-run footer into explicit modern-vs-legacy paths.
  - Frontend:
    - `MonitorScreen` now resolves selected-run footer through a dedicated top-level branch:
      - modern path when `/status` already carries the modern footer markers
      - legacy compatibility path only for footer fields that still arrive without markers
    - modern path now directly resolves:
      - queue footer from run-local `queueStateSource`
      - last-event footer from run-local `lastEventSource`
    - `legacyMonitorFallback` is only consulted from the dedicated legacy branch and no longer sits inline on the main footer path
  - Tests:
    - added a combined modern-path regression proving simultaneous queue + last-event markers prevent all snapshot fallback
    - added a combined legacy-path regression proving missing-marker payloads still route through `legacyMonitorFallback`
    - added a mixed-marker regression proving a queue footer that already has a modern marker keeps its run-local semantics even while an unmarked last-event footer still uses legacy compatibility
  - Verification:
    - ran `npm test -- --run src/screens/MonitorScreen.test.tsx`
- 2026-05-14: Started `P3-4 plugin` realism tightening with the smallest popup page-context slice.
  - Backend:
    - `GET /api/phase3/extension-popup` now prefers persisted scheduler request context for popup page identity:
      - `page.title`
      - `page.url`
      - `page.domain`
    - the same slice also reuses request-context `runtimeMode` and `bodySummary` when present
    - if no persisted scheduler request context exists, the old popup demo defaults remain as the compatibility fallback
  - Frontend:
    - no UI contract change was needed
    - `PluginPopupScreen` already renders `popupSnapshot.page` / `popupSnapshot.runtime`, so the more realistic backend payload flows through directly
  - Tests/docs:
    - backend extension-popup coverage now asserts both:
      - legacy default popup page identity still works
      - persisted scheduler request context overrides the old static page identity
    - synced `plugin/interface-spec.md` and `plugin/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
- 2026-05-14: Continued `P3-4 plugin` with the smallest popup assistive-copy follow-up.
  - Frontend:
    - `PluginPopupScreen` current-page block now prefers `popupSnapshot.summary` for assistive summary copy
    - the old fixed `3 forms / 8 buttons` text remains only as the legacy demo fallback when the popup snapshot omits summary text
    - the old fixed `recognized` badge now reflects `popupSnapshot.status` when available instead of staying fully hardcoded
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - popup summary text overrides the old fixed assistive copy
      - empty summary payloads still fall back to the old demo text
    - synced `plugin/interface-spec.md` and `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-14: Continued `P3-4 plugin` with one more popup host-label cleanup.
  - Frontend:
    - the floating popup header no longer hardcodes `edge.test`
    - it now prefers `popupSnapshot.page.domain`
    - `edge.test` remains only as the legacy fallback when the popup snapshot omits domain text
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup domain hides the old fixed host label
      - missing popup domain still falls back to `edge.test`
    - synced `plugin/interface-spec.md` and `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-14: Continued `P3-4 plugin` with one more fixed active-run status cleanup.
  - Frontend:
    - the `Active run` badge no longer collapses all loaded states into the old binary `running` / `idle` copy
    - it now prefers the concrete `popupSnapshot.runtime.queueState` text
    - `idle` remains only as the legacy fallback when queue state text is absent
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup queue state (`RUNNING`) still renders as `running`
      - missing popup queue state falls back to `idle`
    - synced `plugin/interface-spec.md` and `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-14: Continued `P3-4 plugin` with the smallest selected-element realism follow-up.
  - Backend:
    - `GET /api/phase3/extension-popup` now also exposes additive popup page context fields:
      - `page.locator`
      - `page.actionHints[]`
    - both fields prefer persisted scheduler request context and stay empty when that context is absent
  - Frontend:
    - the popup mirror `Selected element` card now prefers:
      - `actionHints[0]` for the selected label
      - `locator` for the selected meta line
    - the old `Pay $89.10` / `role=button / 140x38px / visible` copy remains only as the legacy demo fallback when those fields are absent
  - Tests/docs:
    - `LocalAdminApiServerTest` now asserts `/extension-popup` returns `locator` and `actionHints[]`
    - `App.test.tsx` now asserts:
      - real popup context overrides the old fixed selected-element copy
      - missing popup context still falls back to the old demo copy
    - synced `plugin/interface-spec.md` and `plugin/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with the smallest candidate-locators realism follow-up.
  - Backend:
    - persisted scheduler request context now preserves additive `locatorCandidates[]`
    - `GET /api/phase3/extension-popup` now surfaces `page.locatorCandidates[]` from that persisted context when present
  - Frontend:
    - the popup mirror `Candidate locators` list now prefers `page.locatorCandidates[]`
    - the old fixed locator list remains only as the legacy demo fallback when popup page context omits candidates
  - Tests/docs:
    - `LocalAdminApiServerTest` now asserts `/extension-popup` returns persisted `locatorCandidates[]`
    - `App.test.tsx` now asserts:
      - real popup locator candidates override the old fixed list
      - missing popup candidates still fall back to the old demo list
    - synced `plugin/interface-spec.md` and `plugin/functional-spec.md`
  - Verification:
    - ran `mvn -pl apps/local-admin-api -Dtest=LocalAdminApiServerTest test`
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one more pick-mode header/state cleanup.
  - Frontend:
    - the popup mirror pick-mode header no longer stays on the fixed `active` badge plus hover/click copy when real locator context exists
    - it now prefers:
      - `popupSnapshot.status` for the pick-mode badge
      - `page.locator` for the pick-mode summary line
    - the old `Hover to highlight / click to select` plus `active` badge remain as the legacy fallback when locator/status context is absent
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup locator context shows `Locator ready for review: ...`
      - missing locator context still falls back to the old hover/click instruction and `active`
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one quick-action subtitle realism cleanup.
  - Frontend:
    - the popup mirror `Open in platform` action no longer stays on the fixed `Full report and logs` subtitle when real popup hints exist
    - it now prefers `popupSnapshot.hints[0]`
    - the old subtitle remains as the legacy fallback when popup hints are absent
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup hints override the old `Full report and logs` copy
      - missing popup hints still fall back to the old subtitle
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one active-run empty-state cleanup.
  - Frontend:
    - the popup mirror active-run card no longer jumps straight to the fixed `No active run` copy when `runtime.nextAction` is absent
    - it now prefers `runtime.auditState` as the headline and uses `runtime.queueState` as the supporting line
    - the fixed `No active run` copy remains only as the final fallback when both runtime fields are absent
  - Tests/docs:
    - `App.test.tsx` now asserts missing `nextAction` still renders real runtime context instead of the fixed empty copy
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one selected-element rationale cleanup.
  - Frontend:
    - the compact selected-element card now shows a supporting locator rationale line
    - it prefers the recommended candidate `reason`, then any available candidate `reason`
    - when popup candidate reasons are absent, it falls back to a fixed review reminder
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup candidate reasons drive the selected-element rationale line
      - missing popup candidate reasons still fall back to the fixed reminder
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one candidate-panel helper-line cleanup.
  - Frontend:
    - the candidate panel now shows a small helper line above the locator list
    - it prefers the current recommended candidate value
    - it is derived only from real popup candidates; when no recommended run-local candidate is available, it falls back to a fixed review reminder
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup candidates drive the helper line
      - fallback/demo candidate rows do not fabricate a top candidate and instead show the fixed review reminder
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one small candidate-review explanation cleanup.
  - Frontend:
    - candidate rows now render real `locatorCandidates[].reason` inline when present
    - the old fixed locator rows now carry lightweight fallback reasons, so the candidate-review area no longer looks explanation-free in legacy/demo mode
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup candidate reasons override demo explanations
      - missing popup candidate reasons still fall back to the demo explanation copy
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one quick-smoke helper-copy cleanup.
  - Frontend:
    - the `Quick smoke test` row now prefers real popup page-path context
    - when `page.url` is present, its helper copy becomes `Run on /...`
    - when popup URL context is absent, it still falls back to the fixed `Run on current URL` demo text
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup page context overrides the fixed quick-smoke helper copy
      - missing popup URL context still falls back to the demo helper text
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one popup-header status-label cleanup.
  - Frontend:
    - the small host-status label in the popup header now prefers real runtime queue context
    - when `runtime.queueState` is present, the header renders `host connected / running`-style copy
    - when runtime queue context is absent, it still falls back to the old environment-style label
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup runtime queue state overrides the old header suffix
      - missing runtime queue context still falls back to `staging`
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one candidate-badge realism cleanup.
  - Frontend:
    - the recommended locator badge now distinguishes real popup candidates from legacy demo rows
    - when run-local `locatorCandidates[]` are present, the preferred row shows `top match`
    - when the popup falls back to demo candidates, the old `recommended` badge remains
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup candidates show `top match`
      - legacy/demo fallback candidates still show `recommended`
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one page-summary helper-copy cleanup.
  - Frontend:
    - the `Page summary` quick-action subtitle now prefers real popup summary text
    - when popup summary context is absent, it still falls back to the fixed `Structured view` demo copy
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real popup summary text overrides the fixed helper label
      - missing popup summary text still falls back to `Structured view`
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
- 2026-05-15: Continued `P3-4 plugin` with one candidate-status realism cleanup.
  - Frontend:
    - low-score real popup locator candidates now surface a `fragile` badge
    - legacy/demo fallback candidates still keep the old fragile marker behavior
  - Tests/docs:
    - `App.test.tsx` now asserts:
      - real low-score candidates render `fragile`
      - legacy/demo fallback list still shows its fragile row
    - synced `plugin/functional-spec.md`
  - Verification:
    - ran `npm test -- --run src/App.test.tsx -t "plugin popup"`
